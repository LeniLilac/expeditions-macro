# Expeditions Macro v1.3.0-beta.36

This silent prerelease fixes a field-observed Quick Placement selection failure that could skip every configured placement before coordinate input. It does not send a Discord release announcement.

## Highlights

- Quick Placement selection proof now follows the complete cyan title/icon through the field-confirmed upward prestart HUD phase. The detector moves the title and icon together and retains every existing cyan population and independent component threshold.
- The retained regression is an unscaled, privacy-reviewed crop from the reported beta.35 run. The full diagnostic archive remains local and is not included in the repository or release.
- Expedition prestart logging now reports normally confirmed placements against the configured count instead of describing skipped rows as sent placements.

## Validation

- The Release solution builds with zero warnings and repository policy passes.
- `1,140` non-golden application tests and `110` complete golden detector tests pass.
- All `5` Deep Debug Viewer tests pass.
- The focused 18-test Quick Placement and bounded placement-attempt gate passes.
- The complete reviewed image corpus rejects every non-Quick-Placement fixture, while both retained Quick Placement phases pass their unchanged structural gates.

## Assets

- `ExpeditionsMacro-1.3.0-beta.36-win-x64-setup.exe` - Windows installer.
- `ExpeditionsMacro-1.3.0-beta.36-win-x64.zip` - portable self-contained build with bundled detector data.
- `dependencies.json` - resolved dependency inventory.
- `SHA256SUMS.txt` - SHA-256 checksums for every release asset.
