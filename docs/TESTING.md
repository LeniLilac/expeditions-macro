# Testing and validation

Use .NET SDK 10.0.302 or a compatible later .NET 10 patch.

## Standard checks

```powershell
./scripts/Test-RepositoryPolicy.ps1
dotnet restore ExpeditionsMacro.slnx --locked-mode
dotnet build ExpeditionsMacro.slnx -c Release --no-restore
dotnet test tests/ExpeditionsMacro.Tests/ExpeditionsMacro.Tests.csproj -c Release --no-build
dotnet test tools/ExpeditionsMacro.DetectorViewer.Tests/ExpeditionsMacro.DetectorViewer.Tests.csproj -c Release --no-build
dotnet test tools/ExpeditionsMacro.DeepDebugViewer.Tests/ExpeditionsMacro.DeepDebugViewer.Tests.csproj -c Release --no-build
git diff --check
```

Start with focused tests while iterating, then expand according to risk. Do not report success from compilation alone.

## Risk-based scope

- Pure documentation or policy text: repository policy check and `git diff --check`.
- Core models/persistence: focused unit tests plus the full non-golden suite.
- Application updates: cover semantic-version precedence, stable/prerelease channel isolation, draft and malformed releases, exact tag/URL/four-asset ownership, missing/duplicate/extra assets, size and dual-digest mismatch, untrusted or excessive redirects, bounded response sizes, cancellation cleanup, tampered/stale stage removal, restart recovery, default-on migration followed by durable opt-out, non-disruptive automatic failure, and explicit download/install consent. Run the full non-golden suite, warning-free Release build, repository policy, and all main-app UI snapshots; visually inspect the update banner and Settings section at 960 by 640 and in both themes.
- Input, capture, coordinator, or cancellation: focused Windows/workflow tests and the full non-golden suite. Manual-input changes must cover malformed schemas, every supported event type, both wheel directions, ignored hotkey transitions, initial pointer state, delayed native hook callbacks, stable same-millisecond ordering, wrap-safe native timestamps, mixed keyboard/mouse ordering, explicit one-pixel action anchors, incomplete-path rejection before save, the allowed one-pixel client-coordinate rounding variance, rejection beyond one pixel without corrective movement, the signed +/- 50-millisecond quality target, continuation on unchanged absolute deadlines below the signed +/- 2,000-millisecond hard stop, rejection at that hard stop before and after send, a dedicated non-thread-pool timing worker, direct uninstrumented live client-bound probes during the timed loop, non-restart classification for a timing miss, focus/geometry loss, cancellation, and held-input cleanup.
- UI/XAML: relevant view-model/page tests, a Release build, and `scripts/Render-UiSnapshots.ps1 -Configuration Release`; inspect every expected snapshot in both themes, including the app-wide update banner and Settings update controls, configured/unset Quick Placement controls and their compact X, empty/nested loop plans, the loop-settings and add-task popups, the two-row task-mode selector and Bounty editor without a parking control, Story Act/Mastery/Infinite option visibility, the Utilities route/interval popup, ready/armed/running Recordings states, the bounded Dashboard run log in wide and compact layouts, the compact stacked Dashboard at the 960 by 640 minimum window, the Placement Setup Recording Mode / Step Mode selector and its stacked narrow header, the required Start Game Match Step with no secondary description and disabled edit/delete actions, collision-free dense placement-marker labels, compact and standard Match Step/Match Settings popups, Settings > Experimental, the dedicated read-only Settings Diagnostics guidance, page-length Match Steps at minimum and medium windows, map-wheel-to-page routing whenever the outer workspace is below its top, and the collapsed app-navigation/Placement-catalog rails.
- Detector Viewer UI: render and inspect dark/light matched, annotation, negative, decode-error, and minimum-size states; run its machine coverage audit; verify image, recursive-folder, repository-dataset, and streamed Deep Debug ZIP input; verify the filename-first picker can address every supported repository fixture; verify frame changes select the filename-owned detector even when its result fails and otherwise use positive production evidence; verify the editable picker follows both Viewer themes; prove annotation rectangles clamp to canonical coordinates, ignore tiny drags, round-trip expected result/notes/labels, autosave atomically by fixture plus detector, and remain unavailable for non-repository sources; and smoke the packaged executable from an unrelated working directory. Every production detector, recognizer, image matcher, and detector-owned vision-metrics surface must map to catalog evidence or an explicit limitation. The catalog discovers image-consuming decision surfaces independently from the coverage audit's explicit production-owner manifest, so a newly named `Matcher`, `Scorer`, or `Metrics` owner cannot silently disappear from both checks. Reflected constants without an exact metric association must remain advisory rather than generating pass/fail.
- Placement playback attempts: prove the default-unset physical Quick Placement and Cancel Placement bindings, left/right modifier identity, compact X clearing, conflict rejection, persistence, and plan-start resolution across direct and legacy setup dependencies. Recording Mode with preserved steps and a Step Mode setup without placement actions must remain exempt. For each contiguous placement batch, prove `Cancel down/up -> Quick Placement down -> placement rows -> Quick Placement up -> Cancel down/up`, with exactly three complete clicks over 50 milliseconds per row and no timed mouse approach or cyan-title detector. Consecutive rows for the same unit must reuse its Quick Placement selection; a changed unit must tap its slot once. Selected-unit proof, targeting, Auto Upgrade, dismissal, callbacks, and between-check delays must begin only after the complete placement batch and trailing Cancel input. Delay, Reconfigure Unit, Upgrade Unit, and Sell Unit actions must split placement batches and execute in author order around the required Start Game boundary. The default total-attempt count is one. Higher counts may re-place and recheck only an unproved row; confirmed rows and the complete batch must never replay. Exhausted proof must log and skip only that row, continue the next action, send no targeting or Auto Upgrade input, omit the successful-placement callback, and avoid runtime/session recovery. Quick Placement and atomic mouse-button release must survive cancellation and exceptions. Authoring, persistence, share, and legacy-load tests must cover the inclusive 1-8 attempt range. Newly authored points still default Auto Upgrade to Priority 1 while missing legacy `auto_upgrade` values remain Off.
- Placement authoring defaults: prove Match settings round-trip the setup-level Targeting and Auto Upgrade defaults, legacy setup files fall back to First/Priority 1, map clicks and newly added Place actions inherit both values, changing the defaults leaves existing actions untouched, and share codes preserve the defaults without exporting app settings. Removing the final user-authored action from a fallback-capable exact Step Mode route must delete the override and restore category resolution; a Start-only category or Recording Mode route remains configured.
- Match Steps authoring and migration: prove exactly one required Start Game step, unrestricted boundary reordering, disabled editing/removal, duplicate rejection, autosave persistence, and complete ordered round trips. Actions above Start Game must resolve before the runner's verified Start click and actions below it afterward. Legacy phase setups must migrate before rows above Start Game and after rows below it, convert absolute after-Start offsets into explicit Delay actions, and preserve equal-offset author order. Current authoring must expose no phase selector or `B`/`A` marker badge. Cover stable duplicate-placement identities, ID-based Reconfigure/Upgrade/Sell selection, cascade removal of every dependent unit action, placement-only map markers, content-sized labels, dense collision-free horizontal and vertical marker lanes, connector/text non-overlap, map-edge bounds, and both themed Placement Setup views. The Sell binding must remain optional unless the resolved timeline contains Sell; playback must prove the selected panel, send one Sell keypress normally, retry no more than twice only while fresh panel ownership remains verified, prove closure, and retire the placement identity. Split advanced controls must preserve legacy combined-proof behavior, skip the default First/Off placement click only through explicit placement-proof opt-out, and keep Sell proof mandatory. Upgrade readiness must require stable green before each press, wait without input through narrow gray, stop remaining presses at the wider Maxed control, and retain a bounded safe failure for missing/unknown panels; its explicit opt-out must send the configured presses directly. Expedition authoring must reject a placement unit slot already used anywhere in the timeline; every placement coordinate must reject the fixed central hotbar/HUD rectangle. Legacy duplicate and HUD-conflicting rows must remain loadable/editable, persist until the user changes them, and be skipped individually at runtime rather than invalidating the complete setup. Runtime tests must prove that an empty or flickering Expedition hotbar slot cannot replace selected-unit proof or invoke the successful-placement callback.
- New Match Step ordering: prove both **Add action** and placement-map authoring append after the complete existing timeline below Start Game, regardless of selection or coordinate, while explicit drag/arrow movement can still place an action before Start Game and existing saved order remains unchanged.
- Unsafe placement coordinates: prove canonical client-edge/default coordinates and the fixed central hotbar/HUD rectangle are rejected during authoring. Legacy rows remain loadable and are skipped before their unit-selection key or mouse input, while later safe rows continue.
- Macro Plan deletion: prove only the requested saved plan file is removed. The UI must confirm the destructive action, flush pending autosave first, select the next saved plan deterministically, persist that selection, and return to a clean unsaved plan when the final saved plan is deleted.
- Autosave lifecycle: use deterministic blocked writes to prove a flush drains edits queued during an active save, plan renames retain their original replacement ancestry, setup shutdown waits for active saves and deletes, stale completions cannot replace newer status, failures remain retryable, and switching suppresses load-generated edits. Recording Mode / Step Mode changes must persist only the recording assignment while preserving every ordinary placement step and route setting.
- Observation-aware UI waits: cover every owned startup, recovery, Lobby, Match Lobby, Settings, Play, Stage, Challenge, Event, Expedition, Team, Refuel, and placement path on a deliberately slow detector clock. Prove an already-observed positive sample is retained, the required fresh confirmations can complete, hard deadlines remain bounded, input-attempt/click/drag caps are unchanged, and no manifest/static action fallback can authorize input without live owner-state proof. Team opening must distinguish Unit Inventory from Unit Teams, use the live Teams action, retry only while the same owner remains stable, and require the destination scrollbar before advancing. Load-confirmation coverage must include the bright Team 1 roster as well as the two-row and bottom-list variants. Operation-session coverage must prove same-Team reuse only inside one user-started operation and one Roblox process, verified switching to another Team, unknown state after a failed/interrupted switch, process-replacement invalidation, and a fresh unknown state after stop/start.
- Fast-only preparation: prove new settings and authoring expose only Fast preparation, zoom/pitch preparation sends no horizontal yaw input, and public-beta camera-model presets remain deserialize-visible but stop with migration guidance before Roblox discovery or input.
- Detector regions, matching, thresholds, ordering, preprocessing, or action placement: focused detector tests and the complete cross-state golden corpus. Any detector using a one-pixel line, border, glyph edge, or similarly fragile feature must prove supported local raster variation through deterministic removal/shift/thickness transforms while retaining independent owner-state evidence. Add variants that brighten or darken mutable artwork, roster, reward, scene, or page-body content without changing the owning controls. OCR coverage must include owner-negative frames containing the same text, ambiguity/confidence failures, and the temporal or semantic constraints required before the result may corroborate progress. Fractional-scale tests must prove mapped half-open subregion edges stay inside their matched owner. Do not satisfy these requirements by broadly lowering a threshold or searching outside the reviewed local neighborhood.
- Match-to-Lobby navigation: both fixed top-bar door offsets, microphone/headset independence, stable same-geometry proof, ignored-click redetection, cancellation before physical input, verified exit confirmation, and final stable Lobby proof.
- Roblox chat normalization: all reviewed open/closed frames, redacted canonical fixtures, adjacent-control negatives, two stable observations before input, no-input closed completion, one ignored-click retry, two fresh closed observations after input, slow observation survival, cancellation, and unknown-state no-input failure. Cover both Lobby acquisition and each mode's prestart boundary.
- Story/Raid option navigation: retained selected-row positives, every wrong option row, Normal/Hard label ownership, delayed target selection, unchanged remembered selection, stable action coordinates, one click per configured option, and safe stop before Select Stage.
- Bounty mode: prove the number recognizer uses only `#1` through `#10` suffix pixels spatially anchored to a live card action across both randomized paper shapes and all five horizontal slots. Cover normal and Beginner's Path-shifted unhovered Event rows, highlighted rows, stable live action ownership, direct board restoration, board entry/exit, Challenge-selector and retained-party handoff through the verified in-match Lobby door, the 200-millisecond reroll cadence, reroll-confirmation ownership, explicit insufficient-Gold banner, 100 ordinary attempts, four unchanged confirmed Mythics, left/right scrolling, an ignored-scroll view mismatch, claim reconciliation, the fixed zero non-viable parking policy, removal of the task-editor control, legacy value discard, early slot-scan completion after every currently retainable unique Mythic is present, ordinary versus daily Challenge blocking, ten-claim UTC reset, unfinished-state persistence, next-Macro-start Gold retry, session-only scheduler exclusion, and an explicit reset when loading schema 1 state with the former Fairy King Forest objective key. Derive usable slots from stable yellow Reroll and green Claim actions in the right-scrolled view, allow a dimmed card at any slot, accept an empty usable set only when the Board detector sees no live actions at all, and send no input to cards with neither action. Require a claimable card's number before input and two fresh Board observations agreeing that the claimed slot settled either dimmed or with a live yellow Reroll action. Both settlements increment the daily claim count and remove active progress; only dimmed settlement excludes that number from the retainable pool until UTC reset. Cover repeated claims of a still-rerollable number without deduplicating the daily claim count. A dimmed-number exclusion must reroll that number if unexpectedly observed again. Operation-session tests must prove that a fresh user start forces the full claim/reconcile/reroll board transaction even when saved work is executable, that the session is consumed only after successful state persistence, and that subsequent scheduler or process-recovery entries retain active-work-first behavior. Planner tests must cover all ten deterministic definitions—including Bounty 2's distinct Flower Forest wave-30 route and Bounty 10's five-Challenge, School Grounds Story Act 1 Hard, and School Grounds Infinite wave-15 work—exact six Placement Setup prerequisites and share dependencies, multi-Bounty same-map overlap without conflating Flower Forest and Fairy King Forest, highest-wave routing, active-work-first claim/reroll deferral after startup reconciliation, wave-target-plus-two exit, valid fixed-size embedded wave templates, exact repeated wave proof, three strictly increasing later readings, no advancement on duplicates, and reset on regressing, low, or unknown wave evidence. Detector changes require the complete cross-state golden corpus.

- Deep Debug rolling retention: use a deterministic clock to prove that PNGs older than the configured 1-through-120-minute tail are absent while every frame record, detector/workflow/input event, and full sanitized run-log line remains in the archive. Verify manifest total/retained/discarded counts, finalization-time pruning after a capture gap, default and clamped settings, Viewer intentional-pruning presentation, legacy missing-frame errors, and Detector Viewer mapping to the retained archive entry after older records were filtered.
- Bounty Challenge and wave-layout extensions: prove that every required Story Infinite setup is validated and adapted to a Challenge-targeted runtime copy without mutating persistence. Exercise every embedded wave template at all three reviewed top-bar locations, the field-derived no-voice and compact-top-bar fixtures, one-pixel phase variation, ambiguity between owned layouts, and independent gameplay-HUD ownership before a recognized number can advance the exit tracker.
- Bounty suffix robustness: exercise exact number templates first, then field-derived raster variants through the bounded action-owned glyph fallback. Require its similarity floor, runner-up margin, and reviewed title-position bound; reject ambiguous blends and otherwise similar glyphs outside a live card-action crop. Run the complete cross-state golden corpus after any threshold, template, or fallback change.
- Dataset changes: privacy review every image, update counts/docs, then run the complete golden corpus.
- Release packaging: `scripts/Build-Release.ps1` and `scripts/Verify-Release.ps1` in addition to the above. Verification must enforce the root-apphost plus nested-dependency layout, require every hash-verified detector payload declared by the bundled manifest, extract under a temporary path containing spaces, launch the root executable from an unrelated working directory, and require a successful packaged UI snapshot run.

## Useful targeted commands

```powershell
dotnet test tests/ExpeditionsMacro.Tests/ExpeditionsMacro.Tests.csproj -c Release --filter "Category!=Golden"
dotnet test tests/ExpeditionsMacro.Tests/ExpeditionsMacro.Tests.csproj -c Release --filter "FullyQualifiedName~DetectorPackGoldenTests"
./scripts/Render-UiSnapshots.ps1 -Configuration Release
dotnet run --project tools/ExpeditionsMacro.DatasetBuilder -- --build datasets/anime-expeditions/expeditions detector-packs <pack-version>
ExpeditionsMacro.DetectorViewer.exe --smoke <output-directory>
ExpeditionsMacro.DetectorViewer.exe --snapshot-ui <output-directory>
```

The golden corpus is intentionally slower and is sharded in GitHub Actions. Local focused checks do not replace the required full corpus after detector changes.

## Regression evidence

- Reproduce a reported failure before changing code.
- For deep-debug archives, generate timestamped contact sheets with `scripts/New-DiagnosticContactSheet.ps1` before examining singleton frames.
- Encode the confirmed state/action sequence as a policy, workflow, or detector test.
- Every detector fix requires a representative privacy-reviewed 808 by 611 fixture plus positive and relevant negative coverage.
- Startup settings work must cover the Lobby gate, the opening-animation negative, UI Scale 0.80/1.00/1.20, device-dependent rendered-scale feedback, all required pages, both Units scrollbar boundaries, both fixed Settings-gear offsets in normal and high-contrast outline forms plus their selected forms, ignored gear actions, no voice-control dependency, and unknown-control safe stops.
- Event navigation work must preserve Act-selector precedence over Event Home, including the shifted full-width selector rail and opaque subtitle, while proving that Event Home still accepts its selected Villain Invasion card plus live Event Gamemode action before decorative header artwork finishes and when either narrow button border rasterizes one row thinner.
- Runtime-recovery changes must prove typed restart-candidate classification, diagnostics before restart, stable Lobby recovery, unchanged incomplete-task progress, operation-scoped startup preflight, and circuit-breaker exhaustion. Do not make generic `InvalidOperationException` recoverable.
- Fast share-code tests must round-trip the plan and loops, referenced Fast presets, exact route overrides or resolved category fallbacks, the ordered Match Steps timeline with exactly one Start Game boundary, unit slots and coordinates, Teams, explicit delays, targeting, per-step Auto Upgrade priority, default timing, route identity, and impossibility thresholds while proving task progress, manual recordings, retired camera-model data, app settings, and secrets are absent.
- Fast share-code tests must also preserve setup-level placement defaults independently of each existing Match Step's configured state.
- Macro-plan persistence tests must prove a legacy task with `"enabled": false` loads as active and that normalized plan/share JSON omits the retired field.
- A plan whose resolved Placement Setup references a manual input recording must fail share export clearly. Tests must reject raw recording payloads and recording references in decoded bundles and prove no local recording can be silently collected or imported.
- When manual user input is paired with passive capture, label the input as user reported rather than claiming it came from the event log.
