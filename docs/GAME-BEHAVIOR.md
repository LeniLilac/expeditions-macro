# Field-observed game behavior

This ledger records Anime Expeditions behavior that has been established from reviewed captures or a prior public build. It keeps automation changes grounded in observed state transitions rather than assumptions about how the game ought to behave.

## Evidence levels

- **Field confirmed**: a timestamped capture and a user-reported or automation-recorded input show the transition.
- **Release retained**: a public app version intentionally used the behavior, but the exact transition has not yet been re-captured.
- **Unverified**: a working hypothesis. It must not override field-confirmed behavior.

## Navigation ledger

### GB-000: Toggle Shift Lock for Fast camera preparation

- Status: **Release retained** for Left Ctrl; configurable physical-key behavior is implemented for users whose in-game binding differs.
- Entry: Fast camera preparation or its isolated Debug action begins with Shift Lock off.
- Action: center the cursor, press the Shift Lock key saved under Controls on the Dashboard, and clamp top-down pitch with vertical relative movement while Roblox owns relative pointer motion. Fast preparation separately clamps maximum zoom and never changes horizontal yaw.
- Exit: press the same snapshotted key during cleanup after success, cancellation, or failure so Shift Lock returns off.
- Key identity: Left/Right Shift and Left/Right Ctrl are distinct physical bindings. The app also supports ordinary letters, numbers, symbols, numpad keys, function keys, and common control keys accepted by the Settings picker.
- Do not: change the configured binding while an operation is running, reuse the macro/Play/Units key, send an arrow pulse or horizontal relative movement, or replace the physical scan-code path with visible absolute cursor motion.
- Protected by: `CameraPosePreparationServiceTests.PrepareWithoutYaw_ClampsPoseWithoutYawInput`, `CameraPosePreparationServiceTests.PrepareWithoutYaw_WhenPitchFails_RestoresShiftLock`, `CameraPosePreparationServiceTests.PreparePitchOnly_PreservesZoomAndYaw`, Shift Lock settings validation, and Windows physical scan-code mapping tests.

### GB-001: Leave a completed match for a different mode

- Status: the terminal-to-party transition is **field confirmed** for Story Victory and user confirmed for Story, Raid, Expedition, and Challenge match-end screens generally. The party-to-selector transition is **field confirmed** independently for all four modes.
- Entry: a Victory or Defeat terminal when the next scheduled task is not the exact same repeatable route.
- Action: press the configured Toggle Play Menu key while the terminal remains open.
- Intermediate: the mode's post-match party appears. Story, Raid, and Expedition parties expose **Change Map** and **Change Gamemode**; the Challenge party exposes **Change Gamemode** without a Change Map action.
- Next action: click the detected **Change Gamemode** control.
- Exit: shared game-mode selector.
- Selector proof: require consecutive shared `play` detector observations when the Story/Raid structural detector cannot classify the selector because an unavailable mode tile is locked and dim. A pending shared-selector observation waits for confirmation and must not trigger another Play-key press. A recognized mode-owned Stage state still takes precedence over this fallback.
- Expedition acknowledgement: after each detected **Change Gamemode** click, discard the party/action proof and require a fresh consecutive proof before retrying. The same verified post-match party may receive at most three clicks while the handoff continues observing for its delayed transition; unclassified frames do not reset that limit, and only a verified transition to another owned state starts a new party-action budget.
- Do not: close the terminal before pressing the Play key, substitute a visible Play-button click for the configured key, assume every mode's party has the same button layout, or require every mode tile to be unlocked before accepting the shared selector.
- Failure rule: after three key attempts without a recognized party or game-mode selector, stop with the Play-binding setup error. A static Expedition post-match party receives no more than three **Change Gamemode** clicks inside the existing bounded handoff observation window.
- Evidence: a manual Story Victory sequence reviewed on 2026-07-22 using v1.3.0-beta.6 shows terminal, Play-key transition, Story party, and shared selector. Four later passive captures begin after the physical Play keypress and independently show Story, Raid, Expedition, and Challenge party-to-selector transitions; they do not contain Victory or Defeat terminals and are not evidence for terminal recognition. The direct terminal keypress path also existed in v1.2.1's Expedition mode-switch workflow. A beta.13 deep-debug run reviewed on 2026-07-24 preserves the current wide School Grounds Defeat panel and confirms that the background hotbar Play control and **Game Results** button must not replace terminal recognition. A second beta.13 run preserves consecutive Rose Kingdom Victory frames whose animated cyan reward artwork intermittently erased the former generic panel score while the independent red Close and wide purple **View Party** actions remained live; current recognition deliberately ignores animated reward artwork, roster size, reward values, and reward colors. A beta.31 Deep Debug Story run reviewed on 2026-07-27 records one correctly injected `P` opening the shared selector, whose bundled `play` score remained `0.926-0.930`; the Stage detector returned `None` only because the account's locked Raid tile removed the normally red heading evidence, after which the old workflow injected `P` twice more and raised a false binding error.
- Protected by: `StageHandoffPolicyTests.DifferentModeVictory_UsesTheFieldObservedPlayMenuSequence`, `StageHandoffPolicyTests.LockedRaidSelector_UsesStableSharedPlayEvidenceWithoutRetryingTheKey`, `ExpeditionRunPolicyTests.CompletedRunHandoff_UsesOnlyStateOwnedActions`, `ExpeditionGameModeHandoffLoopTests`, `ChallengeScreenDetectorTests.CurrentWideDefeatPanel_WinsOverBackgroundHudControls`, `ChallengeScreenDetectorTests.AnimatedVictoryRewards_RemainStableAcrossConsecutiveFrames`, and Challenge handoff policy tests.

### GB-002: Repeat an identical scheduled route

- Status: **Field confirmed**.
- Entry: terminal screen with the next eligible task resolved to the exact same mode and preset.
- Action: click the detected **Repeat Stage** control.
- Exit: the same route's prestart screen.
- Preparation reuse: after the Repeat Stage transition reaches a verified prestart screen, preserve the already-loaded team and Fast-prepared zoom/pitch pose. Do not reopen Units or repeat Fast camera preparation before the next match.
- Invalidation: Lobby, AFK Chamber, disconnect, rejoin, recovery navigation, a new Roblox PID, or a new top-level Macro operation invalidates the Fast pose. A verified post-match Play handoff can preserve it across a different mode or map as specified by GB-024, but a different team/preset or any recovery invalidates the loaded-team assumption.
- Do not: reopen the Play interface or repeat unchanged preparation for an identical uninterrupted route.
- Exception: Challenge victories always return through Play because the rotation can advance to a different stage. A Challenge defeat uses Repeat Stage only when a configured retry remains.
- Protected by: scheduler continuation, `RepeatedRoutePreparationStateTests`, and Challenge continuation policy tests.

### GB-003: Load a saved team

- Status: **Field confirmed**.
- Entry: a recognized mode-specific prestart screen.
- Action: open Units with the configured Units key, select Teams, load the configured slot, handle the equipment confirmation, then close Units.
- Exit: the same prestart screen.
- Do not: start team loading from the shared Play interface.
- Protected by: `StageScreenDetectorTests.TeamLoadGuard_RequiresPrestartAndRejectsPlaySelector` and team-selection tests.

### GB-004: Return to the lobby from a match

- Status: **Field confirmed**.
- Entry: an active match, including its Victory or Defeat terminal.
- Play-interface prerequisite: the top **Back to Lobby** control is unavailable while the post-match Play interface owns the screen. If Play is open, click its detected bottom-left **Back** action and verify each resulting layer before clicking again. A Challenge handoff can require two actions: shared game-mode selector to retained Challenge party, then retained party to the underlying match.
- Control: detect the opaque white door glyph in the match-owned top bar. The fixed 808 by 611 Roblox bar has two supported layouts: without the optional voice slot the door glyph spans `x=260–279`, and with that slot it spans `x=304–323`. Ignore whether the optional slot renders a microphone or headset. Require two consecutive observations of the same exact door action at `(270, 35)` or `(314, 35)`, then mouse-click it through acknowledged motion.
- Intermediate: an **Exit Confirmation** modal asks whether the player wants to exit the game. It contains a red **Return to Lobby** action and a gray **Cancel** action.
- Next action: detect the modal in consecutive captures, then mouse-click its detected red **Return to Lobby** action. Require consecutive captures where the modal is absent before waiting for Lobby.
- Exit: Roblox enters its teleport/loading sequence and eventually reaches a stable Lobby.
- Do not: press the Toggle Play Menu key to close nested post-match Play layers, inspect or depend on the optional voice-control glyph, scale the top-bar coordinates from Anime Expeditions UI Scale, click either fixed door coordinate without the matching live glyph, use accessibility navigation, reuse a terminal close-button coordinate, or continue navigation before the modal closure and Lobby are each verified.
- Failure rule: retry the detector-owned red action once if the verified modal remains open. A missing modal, rejected action, changed Roblox session, or unstable Lobby is a recoverable Roblox UI/session failure.
- Unverified: the post-Cancel state was not exercised and must not be assumed.
- Evidence: user-described manual input and passive captures reviewed on 2026-07-22 and 2026-07-26 preserve the shared game-mode selector, retained Challenge party, bottom-left Back actions, confirmation, and subsequent teleport. Seven additional canonical capture sets reviewed on 2026-07-27 contain 228 top-bar observations across moving maps and both fixed offsets. The white door, arrow, and handle geometry remain opaque and identical while the semi-transparent surround and optional microphone/headset vary.
- Protected by: `MatchLobbyDoorButtonDetectorTests`, `LobbyExitConfirmationDetectorTests`, `MatchLobbyNavigatorTests.ReturnClicksStableDoorThenDetectedConfirmation`, ignored-click/cancellation safety tests, and `PlayInterfaceCloserTests.ChallengeSelectorHandoff_ClicksBackThroughPartyBeforeLobbyNavigation`.

### GB-005: Prevent an intentional long idle from reaching the AFK Chamber

- Status: **Field confirmed** in a beta.8 Deep Debug run: an inactive Infinite match entered the AFK Chamber roughly every 18 minutes. The exact game-side timeout remains account/server dependent.
- Existing recovery: the app already detects the AFK Chamber, chooses **Return to Lobby**, verifies the lobby, and navigates back to the configured route.
- Product rule: when a task intentionally waits longer than ten minutes, perform a keepalive before ten minutes elapse and repeat it while the wait continues. Use an eight-minute cadence so timing jitter cannot cross the ten-minute safety boundary.
- Preferred action: focus and revalidate Roblox, verify a known non-text idle state, then send one ordinary `O` key pulse through the shared keyboard-input path.
- Why `O`: Roblox uses `O` for Zoom Out, and this app already uses that binding during camera preparation. At the fully zoomed-out limit it is effectively idempotent, while an arbitrary click could activate a UI control.
- Preconditions: do not send the pulse while a text field may own keyboard focus, during a transition, or while another workflow owns input. Fast workflows must still perform their normal zoom/pitch preparation afterward rather than assuming the keepalive established camera state.
- Do not: use a blind mouse click as the default keepalive, wait until the ten-minute boundary, or send only one pulse for a multi-hour wait.
- Failure rule: if Roblox cannot be focused or the pulse cannot be sent, defer it for one minute without stopping the active workflow; retain normal AFK-Chamber recovery as the final fallback.
- Evidence: a 2h17m beta.8 Deep Debug archive contained four AFK-Chamber transfers and zero `O` events, proving the prior policy had not been wired into runtime. The final 200 frames also show that Play-key attempts during Return-to-Lobby loading can consume all retries before lobby appears.
- Protected by: `InactivityKeepAliveTests`, active-match integration in every mode runner, Challenge cooldown waiting, and the Stage handoff policy that suppresses Play-key input until an AFK return reaches a verified navigation destination.

### GB-006: Enter a Story route from the shared game-mode selector

- Status: **Field confirmed** for the captured Mastery route; the user confirmed Act, Infinite, and Mastery as the selectable run types.
- Entry: shared game-mode selector.
- Actions and verified states:
  1. Click the detected **Story** mode tile and verify the Story map selector.
  2. Click the configured Story map and verify its detail panel.
  3. Click the configured Act, **Infinite**, or **Mastery** option once. Require its left-rail selected row across consecutive observations before continuing. Accumulate the visually one-pixel rail per scanline across only its strongest three-column band so adjacent raster coverage cannot erase ownership; retain the complete detail owner, one distinct selected row, configured difficulty where applicable, and live **Select Stage** action. For an Act, prove the configured act row first, click Normal or Hard once, then require the same act row plus the fixed green **Normal Mode** or red **Hard Mode** label.
  4. Click the detected **Select Stage** action and verify a launch-ready Story party preview as described in GB-011.
  5. Click the detected **Start** action and wait through teleport/loading until the Story prestart screen is verified.
- Difficulty scope: only Story Acts expose Normal/Hard. Infinite and Mastery have no difficulty action; the authoring UI hides Hard mode for them, execution sends no difficulty input, and loading, listing, saving, or sharing legacy Infinite/Mastery data normalizes any stale `hard_mode: true` value to false. Story Act values remain unchanged.
- Do not: reuse one run-type accent as the detector for all Story variants, accept the unchanged generic detail panel after an option click, retry an ignored option click blindly, send a difficulty click for Infinite or Mastery, or click **Select Stage** from a wrong/ambiguous selected row. Act, Infinite, and Mastery use different accent colors and layouts.
- Evidence: a 21-frame passive diagnostic capture reviewed on 2026-07-22 using v1.3.0-beta.6. Physical clicks are user reported; the capture shows every resulting screen from the shared selector through Story prestart. Retained detail fixtures positively cover selected Act 1, Act 4, Infinite, and Mastery rows plus Normal and Hard labels; Acts 2, 3, and 5 use the identical repeated row geometry visible as negatives in those panels.
- Protected by: `StageOptionSelectionDetectorTests.ReviewedStoryDetails_ReportSelectedOption`, `StageOptionSelectionDetectorTests.StoryDetails_RejectEveryWrongOptionRow`, `StageOptionSelectionDetectorTests.WrongDifficulty_DoesNotAuthorizeSelectStage`, `StoryHardModeNormalizationTests.MacroPlanRepository_LoadListAndSaveNormalizeOnlyNonActStoryTasks`, `StoryHardModeNormalizationTests.StoryPresetRepository_LoadListAndSaveNormalizeOnlyNonActPresets`, `StoryHardModeNormalizationTests.ShareCodec_LoadsLegacyHardFlagsAndWritesCanonicalStoryData`, current Story screen/action detector tests, and Story navigation tests.

### GB-007: Enter a Raid route from the shared game-mode selector

- Status: **Field confirmed** for Spirit City and its captured act selection.
- Entry: shared game-mode selector.
- Actions and verified states:
  1. Click the detected **Raid** mode tile and verify the Raid map selector.
  2. Click the configured Raid map and verify its detail panel from the shared red header, top accent edge, dark body, and independently detected live **Select Stage** action. Do not require the decorative Close circle to remain an isolated component; the current panel border can connect to it under both Roblox font settings.
  3. Click the configured Raid act once and require its distinct red selected row across consecutive observations. Accumulate the visually one-pixel rail per scanline across only its strongest three-column band while retaining the complete detail owner and live **Select Stage** action.
  4. Click the detected **Select Stage** action and verify a launch-ready Raid party preview as described in GB-011.
  5. Click the detected **Start** action and wait through teleport/loading until the Raid prestart screen is verified.
- Do not: accept the unchanged generic Raid detail panel after the act click, retry an ignored act click blindly, or click **Select Stage** until the requested row and live action agree.
- Evidence: a 17-frame passive diagnostic capture reviewed on 2026-07-22 using v1.3.0-beta.6. Physical clicks are user reported; the capture shows the shared selector, Raid map selector, act changes, party preview, teleport, and prestart. Two beta.13 Deep Debug traces reviewed on 2026-07-24 show the current Spirit City detail panel with Roblox's custom and default fonts. All three retained panels positively cover selected Act 1 and expose Act 2 and Act 3 as repeated dark rows; the remaining selected positions reuse that same row geometry without fabricated fixtures. Both current panels expose the same live **Select Stage** action, while their red Close circles merge with the panel border and therefore cannot be required as isolated button components.
- Protected by: `StageOptionSelectionDetectorTests.ReviewedRaidDetails_ReportOnlySelectedAct`, `StageOptionSelectionDetectorTests.UnchangedPreClickFrames_NeverAuthorizeWrongRoute`, `StageOptionSelectionDetectorTests.DelayedTargetSelection_ExtendsPendingProofPastSoftBoundary`, current Raid screen/action detector tests, and Raid navigation tests.

### GB-008: Enter an Expedition route from the shared game-mode selector

- Status: **Field confirmed**.
- Entry: shared game-mode selector.
- Actions and verified states:
  1. Click the detected **Expedition** mode tile and verify the Expedition map-selection screen.
  2. Select the configured map from the current left-side card rail and verify its cyan active perimeter. Retain the prior compact selector as a supported legacy layout.
  3. Select the configured difficulty from the current lower-left control and wait for its color transition to settle before verifying the active green, red, or purple value.
  4. Click the detected **Select Stage** action and verify a launch-ready Expedition party preview as described in GB-011.
  5. Click the detected **Start** action and wait through teleport/loading until the Expedition prestart screen is verified.
- Recovery failure rule: automatic recovery may retry one unchanged, verified owner screen at most three times and may perform at most eighteen action attempts across the complete rejoin. After every input, discard its state/action stability and require fresh consecutive live proof before retrying. While that transition is pending, send neither a duplicate nor an alternate navigation action. Unclassified frames and a different candidate action do not reset the owner budget; only a stably verified transition to a different owned state does. If Roblox ignores an action, never finishes loading the next screen, or cycles between screens until either limit is exhausted, stop with a recoverable UI error naming the last state. Cancellation stops before another recovery action.
- Slow transition rule: after a verified recovery action is sent, the resulting transition remains pending through unclassified teleport/loading frames and may use the observation budget's existing bounded hard grace. Do not retry the previous owner action merely because the soft loading window elapsed.
- Evidence: a 16-frame passive diagnostic capture reviewed on 2026-07-22 using v1.3.0-beta.6 covers the prior selector. A second passive capture reviewed on 2026-07-25 covers all three current map cards, all three difficulties across multiple maps, the current **Select Stage** action, and an unchanged launch preview. A beta.42 automatic recovery archive reviewed on 2026-07-30 shows a verified map-preview Start click followed by 32 seconds of unclassified loading and then a stable 99.7% prestart screen; the old soft timeout replayed the stale map-preview action instead of accepting that delayed transition.
- Protected by: Expedition recovery/navigation, current-and-legacy map-selection, difficulty-stability, preview, and prestart tests, including `CurrentExpeditionSelectorTests.Selector_ReportsLiveStateAndActions`, `ExpeditionRecoveryTransitionLoopTests.StaticStateNeverExceedsThePerStateAttemptCap`, `ExpeditionRecoveryTransitionLoopTests.ARealStateChangeResetsTheConsecutiveStateBudget`, `ExpeditionRecoveryTransitionLoopTests.CyclingStatesStillStopsAtTheOverallAttemptCap`, `ObservationWaitBudgetTests.PendingRecoveryLoadUsesTheExistingHardGrace`, and `ObservationWaitBudgetTests.ExpeditionActionsRequireTheirOwningScreen`.

### GB-009: Enter an eligible Challenge from the shared game-mode selector

- Status: **Field confirmed** for the captured Regular Challenge route and user confirmed for the cooldown/type-selection process.
- Entry: shared game-mode selector.
- Actions and verified states:
  1. Click the detected **Challenge** mode tile and verify the Challenge selector.
  2. Inspect the configured Challenge types and their cooldown or daily-limit state. Do not select a type that is unavailable.
  3. Select the eligible Challenge type and configured challenge row, then verify its detail panel.
  4. Click the detected **Select Stage** action and verify a launch-ready Challenge party preview as described in GB-011.
  5. Click the detected **Start** action and wait through teleport/loading until the Challenge prestart screen is verified.
- State-ownership rule: upgrade reward cards exist only in Expedition matches. Expedition monitoring owns the `reward` state; shared Story, Raid, Challenge, and Event prestart detection must never consult that matcher or let it suppress a valid **Start Game** dialog. The Expedition-only detector still requires the real dark **Select Upgrade** action rails, so colorful map scenery cannot become an actionable reward state.
- Load-failure rule: if the bounded wait expires after a Teleporting transition, report the last current state rather than claiming Roblox is still teleporting. Treat the exhausted stage load as a session-level failure so configured private-server recovery can restart Roblox and retry the same incomplete task without progress.
- Unavailable rule: when all configured Challenge types are on cooldown or exhausted, leave the selector through its verified close action before returning control to the scheduler or waiting.
- Daily-limit rule: persist attempted Challenge types, the current half-hour reset epoch, and unavailable-rotation evidence in Macro Plan progress. Scheduler handoffs, Stop/Start, and an application restart therefore resume the same rotation instead of repeating an already attempted type or losing daily-limit evidence. A new half-hour epoch clears attempted types; `00:00 UTC` clears daily-limit evidence. If every regular Challenge remains unavailable after a complete global half-hour reset, treat the account's daily limits as exhausted until that UTC reset and run the next eligible task instead of probing Challenges every half hour.
- Detail-action rule: carry the stable `ChallengeAvailable` frame and live **Select Stage** action into final click revalidation. One slow or obscured recheck may reset that seed and begin a fresh stable proof; an action owned by cooldown or another screen never satisfies it.
- Detail-return rule: after clicking **Back** from an available or cooldown detail, wait for a stable Challenge selector before clicking again. The detail can remain visible while the first click is transitioning; a second immediate Back can land on the restored list and reopen a challenge.
- Navigation retry rule: after each verified Challenge tile, post-match **Change Gamemode**, AFK **Return to Lobby**, or Disconnect **Reconnect** input, discard its prior stability proof and require fresh consecutive proof before another attempt. Retry one unchanged owned state at most three times; reset that budget only after a different owned state is stably verified. A pending slow transition blocks duplicate and alternate navigation input, cancellation stops before another attempt, and an exhausted unchanged state stops with a recoverable state-specific UI error.
- Evidence: a 16-frame passive diagnostic capture reviewed on 2026-07-22 using v1.3.0-beta.6 shows the successful route. A 61-frame beta.9 deep-debug run reviewed on 2026-07-23 records the delayed detail-to-selector transition and the second Back click reopening a challenge. A beta.14 Deep Debug run reviewed on 2026-07-24 records all regular Challenges unavailable across multiple reset epochs while invocation-local state incorrectly scheduled another half-hour probe. A beta.18 Deep Debug run reviewed on 2026-07-25 records one valid Teleporting frame followed by 270 stable Flower Forest prestart observations whose colorful scenery falsely satisfied an Expedition-only reward-card heuristic inherited by the Challenge path. A beta.27 Deep Debug run reviewed on 2026-07-26 records the same loaded Flower Forest prestart at 98% Start-dialog confidence while the unrelated reward matcher also scored 97%; the Start dialog remains the owned state.
- Protected by: Challenge selector, cooldown, preview, handoff, and scheduler tests, including `ChallengeNavigationInputGateTests.StaticScreen_StopsAfterThreeVerifiedInputAttempts`, `ChallengeNavigationInputGateTests.VerifiedStateChanges_ResetTheAttemptBudget`, `ChallengeNavigationInputGateTests.PendingSlowConfirmation_DoesNotAuthorizeDuplicateInput`, `ChallengeActionRetryTests.SlowActionProof_CannotExceedMaximumParkingAttempts`, `ChallengeActionRetryTests.SeededAvailableAction_ReacquiresAfterSlowObscuredFrame`, `ChallengeActionRetryTests.SeededAvailableAction_RejectsChangedOwnerAction`, `ChallengeMacroRunnerTests.ChallengeDetailBack_WaitsThroughStaleFramesBeforeAnotherClick`, `ChallengeMacroRunnerTests.LoadedUnknownScreen_AfterTeleportUsesRuntimeRecovery`, `ChallengeScreenDetectorTests.ColorfulFlowerForestScenery_DoesNotSuppressTheStartDialog`, `DetectorPackGoldenTests.RewardSelection_RequiresLiveActionRailsOverColorfulScenery`, and `ChallengeRunPolicyTests.SeparateScheduledInvocations_SharedStateInfersDailyLimitUntilMidnightUtc`.

### GB-010: Mode details differ between lobby and post-match party contexts

- Status: **Field confirmed** for Story, Raid, Challenge, and Expedition.
- Entry: a mode detail or map-selection screen reached from the shared game-mode selector.
- Lobby context: a fresh party exposes **Select Stage** beside the purple **Enter Matchmaking** action.
- Post-match context: pressing the configured Play key on a terminal and choosing **Change Gamemode** preserves the existing party. The resulting mode detail omits **Enter Matchmaking** and retains or expands **Select Stage**.
- Detector rule: identify the mode from its stable detail structure and require the live **Select Stage** action. Treat **Enter Matchmaking** as optional corroboration, never as a state-defining requirement.
- Action rule: click the detected **Select Stage** component because its center and width differ between the two contexts.
- Do not: require the purple action, use its absence as a cooldown/error signal, or reuse a fixed Select Stage coordinate across the narrow and wide rails.
- Evidence: two passive diagnostic captures reviewed on 2026-07-22 provide lobby and post-match detail frames for all four modes. A reported Raid Act 2 failure proves the strict two-button gate blocked act selection before any Act 2 click was sent.
- Protected by: `ModeDetailVariantTests` and the retained fixtures under `datasets/anime-expeditions/navigation-variants/`.

### GB-011: Launch previews differ between lobby and retained post-match parties

- Status: **Field confirmed** for Story Act, Story Infinite, Story Mastery, Raid, Challenge, and Expedition in both party contexts.
- Entry: a mode's **Select Stage** action has been clicked and its party preview is visible.
- Lobby family: the action rail contains **Start**, an optional **Change Map**, and **Disband**.
- Post-match family: the retained party action rail contains **Start**, an optional **Change Map**, and **Change Gamemode**.
- Launch rule: treat either family as launch-ready only when the live green **Start** component is independently detected across the configured stable-frame count, then click that detected component.
- Handoff rule: keep the families distinct outside launch. **Change Gamemode** is the verified path back to the shared selector; **Disband** is not interchangeable with it.
- Do not: require **Disband** before starting, count another preview state's action as Start stability, click the `PostMatchPreview` state's Change Gamemode action when the current workflow is waiting to launch, or use a fixed Start coordinate without detecting the live button.
- Failure rule: a party panel may finish rendering before its green Start action becomes live. Keep observing within the bounded, slow-device-aware navigation budget; if Start never becomes actionable, stop without clicking any other action on that panel.
- Evidence: one failing beta.7 deep-debug Story run and two passive beta.7 diagnostic captures reviewed on 2026-07-22. The deep log records Select Stage followed by repeated 94% `PostMatchPreview` recognition with no Start click; the passive captures cover all six listed route variants in both contexts. A beta.31 Deep Debug Challenge run reviewed on 2026-07-27 records School Grounds detail opening before its live Start action: three stable `PostMatchPreview` / Change Gamemode observations incorrectly completed the old `PreviewReady` action waiter.
- Protected by: `StageHandoffPolicyTests.PreviewWait_AcceptsEitherPartyFamilyOnlyWithADetectedStartAction`, `StageScreenDetectorTests.BothPartyPreviewFamilies_MapTheLiveStartButton`, `ChallengeMacroRunnerTests.PreviewStartWaiter_WaitsForTheExpectedLiveAction`, and `ChallengeMacroRunnerTests.PreviewStartWaiter_NoLiveActionTimesOutWithoutClickTarget`.

### GB-012: A prestart UI can load without stage geometry

- Status: **Field confirmed**.
- Entry: an Expedition prestart screen after the route has loaded.
- Visible failure: the Start Game dialog, hotbar, and HUD are present, but the world behind them remains a nearly uniform blue field rather than rendering map geometry.
- Action: do not rotate the camera, place units, or click Start. Open Play with the configured binding, return through the existing party flow, and select the same configured route again.
- Exit: retry camera preparation only after the saved world regions contain stable rendered geometry.
- Escalation: if bounded in-client recovery fails and private-server restart is configured, close only the verified Roblox player process, relaunch the saved server through the registered Roblox protocol, reload the saved plan, and retry the same incomplete task.
- Do not: report this as ordinary low camera confidence, consume camera-alignment attempts, increment task progress, or continue placement over the missing world.
- Evidence: a six-hour beta.8 Deep Debug run reviewed on 2026-07-23. The blue void began around frame 27,092; the same run eventually returned through Play, re-entered the route, and loaded normally.
- Protected by: `CameraWorldReadinessTests.BlueVoid_IsRejectedWhileRenderedMapRemainsReady`, `RobloxRuntimeRecoveryPolicyTests`, and `RecoveringMacroSchedulerTests.RuntimeFailure_RestartsRobloxAndRetriesTheIncompleteTask`.

### GB-013: A stage teleport can briefly pause window capture

- Status: **Field confirmed**.
- Entry: a launch-ready party has accepted **Start** and Roblox is switching through its experience splash and stage teleport screens.
- Observed failure: the Roblox window, PID, and canonical 808 by 611 client remain valid, but Windows Graphics Capture can deliver no post-barrier compositor frame for longer than the normal one-second fresh-frame deadline.
- Action: dispose the stalled capture session, create one replacement against the same verified Roblox window and live client geometry, and wait through its bounded initial-frame allowance.
- Exit: resume ordinary detector polling from the first fresh replacement-session frame without repeating **Start**, another navigation click, or any camera input.
- Failure rule: if the replacement session also times out, report the existing session-level capture failure so configured outer recovery can take over.
- Do not: return a cached pre-teleport frame, weaken the post-input freshness barrier, resend the launch input, or retry capture without a bound.
- Evidence: a beta.12 automatic error archive reviewed on 2026-07-23 shows successful School Grounds Infinite navigation followed by a capture failure during the experience splash; its post-failure frames reach the correct prestart five seconds later. A Deep Debug retry on the same PID records the complete splash-to-teleport-to-prestart sequence without a navigation change. A separately replicated Spirit City Raid 2 Deep Debug run records one Start click, the same valid Roblox PID and 808 by 611 client, then the exact post-barrier `TimeoutException` while the teleport continued normally in failure-diagnostic frames.
- Protected by: `WindowsRobloxAutomationTests.WindowCapture_TransientFreshFrameTimeoutRecreatesSessionOnce` and `WindowCapture_RepeatedFreshFrameTimeoutRemainsBounded`.

### GB-014: Align saved Unit Teams through the scrollbar thumb

- Status: **Field confirmed** for Teams 1 through 8.
- Entry: the configured Units key has opened either Unit Inventory or the Unit Teams list at the canonical client size. Reopening Unit Teams resets its scroll position to the top.
- Opening transition: Unit Inventory owns its red Close, red **Unequip All**, purple **Teams**, and yellow **Quick Sell** actions. Its decorative gold title and variable roster/body brightness are confidence only. Click the live Teams action only after consecutive Unit Inventory observations. The destination requires that red Close control, at least two full **Load Team** rows, and the real height-bounded scrollbar anchored to the panel across consecutive frames. If the same Unit Inventory and Teams action remain stable, retry that action once; unknown or moving evidence waits without input inside the same hard bound. The Unit Teams panel and thumb slide upward from the bottom. The surrounding Roblox scene can expose a much taller neutral-gray strip beside the panel, so gray color alone does not identify the thumb.
- Action: locate the live gray scrollbar thumb three to nine pixels right of the detected red Close-control center, reject candidates outside the field-observed 60–95 pixel height range, and require consecutive matching thumb geometry before acting. If a settled reopening is not at the top, repeatedly over-drag the detected thumb above the track within a ten-second deadline so Roblox clamps it at the verified top position, then re-verify it. Hold the left mouse button on that thumb and drag it to the requested team's absolute alignment.
- Alignment: Teams 1 through 6 each align as the first fully visible row and use that row's detected green **Load Team** button. Teams 7 and 8 over-drag below the track so Roblox clamps the thumb at the shared bottom limit, then use the second and third fully visible buttons respectively.
- Exit: require consecutive aligned frames with a full-height Load Team button, click the detected button, and verify the Load Team confirmation before continuing. The clicked or current row can leave exactly two independently visible Load Team rows; those rows plus the independently detected live modal action or actions own the result. A bright populated roster may reduce whole-panel darkness while the modal is open, so darkness remains confidence only. A one-row raster gap may not split an otherwise field-shaped scrollbar thumb.
- Operation reuse: one user-started Macro operation remembers the last positively loaded Team together with the owning Roblox process. A later Story, Raid, Event, Challenge, Expedition, or Bounty route requesting that same Team may skip reopening Units. Before requesting another Team, clear the remembered value; record the new Team only after the complete load transaction succeeds. A failed or interrupted switch therefore leaves the operation Team unknown instead of reusing stale knowledge. A replaced Roblox process invalidates the remembered Team, and every stop/new user start begins unknown because the user may have changed Teams between operations.
- Do not: infer Unit Teams from green unit-card artwork, advance without its real scrollbar, use a static Teams-tab coordinate, wheel-scroll with the cursor over unit cards, click a clipped third row, rely on scroll position persisting after the interface closes, or assume the older and current panel widths place the scrollbar at the same X coordinate.
- Failure rule: the Unit Inventory-to-Teams transition permits at most two verified live-action clicks. If Unit Inventory remains open after both, or neither owned screen settles, stop with a recoverable Roblox UI failure. Within a fifteen-second target-alignment deadline, keep re-detecting the live thumb and requested full-height Load button. If the button is not yet actionable, drag again toward the target even when the thumb is merely near it. Never use a fixed adjustment count or click a neighboring/clipped row.
- Evidence: three beta.13 team-selection runs and two passive manual-navigation captures reviewed on 2026-07-23. Team 3 failed because its fixed `(580, 447)` action landed below a clipped button. The manual drag sequence establishes one aligned position per Team 1–6 and the shared bottom limit for Teams 7–8. Physical manual drags are user reported; the passive captures preserve their resulting positions. A later macro trace reviewed on 2026-07-24 shows the opening thumb rising to center `240` while an unrelated gray background run at approximately `x=644`, `y=190–435` caused the old longest-run heuristic to report center `312`. Two beta.15 Debug runs reached and clicked the correct Team 7/8 buttons, but the confirmation detector rejected the visible dialog because one underlying green row covered 9.8% rather than the former 10% requirement. Five beta.16 Team 8 failures and two successes establish two additional boundaries: scenery at `x=644–650` can form a valid-height gray run while the real top thumb remains at `x=628`, and a drag ending at the desired bottom center can under-travel to centers `341–380`. Only the successful controls reached the clamped bottom center `396`; at center `373`, the apparent third action region still belongs to Team 7. A beta.20 Team 3 trace reviewed on 2026-07-25 records two accepted drags whose second result settles seven pixels above the nominal center while Team 3's Load button is already fully visible and stable. The live row band must include its full button, and a near-target tolerance must accept this Roblox snap. Two beta.31 Team 4 Debug traces reviewed on 2026-07-27 record successful clicks at `(579, 285)` followed by the correct Load Team modal over exactly two full underlying green rows; scoring those two required rows as two-thirds of an assumed three made the valid confirmation mathematically unable to pass.
- Later evidence: a beta.41 desktop video reviewed on 2026-07-30 shows a stable Unit Inventory being classified as Unit Teams at 72.9% because green unit artwork overlapped two nominal Load-button rows. The workflow skipped the Teams action, reported **Loading Team 4**, and then failed because no Teams scrollbar existed. That frame's live Teams action is `(305,452)` while the retired static point was `(305,427)`. A beta.44 Debug Team 3 run reviewed the same day exposed a second roster-content boundary: every live Unit Inventory action scored between 94% and 97%, but the populated bright/red card grid reduced whole-panel darkness to 36.9% through 38.7%, below the former 42% gate. Only the sparse loading frame was accepted, so the two-observation owner proof could never authorize the Teams click. A beta.45 Debug Team 1 run then clicked the correct `(579,267)` Load Team action and displayed a stable confirmation for the full timeout. Its live Confirm action scored 98.7% through 99.1% and three Load Team rows remained visible, but bright roster content reduced the dimmed panel to 79.9% through 80.3%; weighting that darkness as nearly half the owner proof made every valid modal classify as `None`.
- Protected by: `TeamSelectionServiceTests.Select_AlignsAndLoadsEveryTeamWithoutWheelScrolling`, `Select_FieldUnitInventoryUsesItsLiveTeamsAction`, `Select_RetriesWhenVerifiedUnitInventoryRemainsOpen`, `Select_StopsAfterTwoIgnoredVerifiedTeamsActions`, `Select_SlowOwnerObservationsCompleteTheVerifiedTransition`, `Select_WaitsForTheOpeningAnimationAndUsesTheRealTopThumb`, `Select_NormalizesAReopenedScrolledListBeforeLoading`, `Select_AcceptsAStableNearTargetScrollbarUndershoot`, `Select_LowerTeamsDragPastTheTrackSoRobloxClampsAtBottom`, `TeamScreenDetectorTests.FieldUnitInventory_DoesNotBorrowGreenTeamRows`, `Team4TwoRowLoadConfirm_UsesTheLiveConfirmAction`, `Team1BrightRosterLoadConfirm_UsesTheLiveConfirmAction`, `TeamOperationSessionTests`, `RepeatedRoutePreparationStateTests.OperationKnownTeam_IsReadyOnTheFirstRouteAttempt`, and the aligned/opening/background-decoy cases in `TeamScreenDetectorTests`.

### GB-015: Retired camera-model behavior — grouped coarse correction

- Status: **Historical field evidence; retired in v1.3.0-beta.33**. This entry records how the former public camera-model workflow behaved; it is not an active product contract.
- Historical entry: the stabilized runtime view had either a registered match or a strong, isolated dense fingerprint match in a saved full-turn yaw atlas but did not already match the goal.
- Historical action: choose the shortest direction, send no more than six arrow pulses, capture the resulting pose, and recalculate the shortest correction from the newly observed atlas position.
- Historical dense-evidence rule: a below-threshold registered match could earn only this bounded feedback path when it retained at least 20% structural evidence, its fingerprint was at least 94%, and that fingerprint was at least six percentage points above every atlas position outside a density-scaled angular neighborhood. During setup restoration only, a verified prior position plus bounded input could replace global isolation for the next physically constrained lookup; it still could not complete setup without direct goal verification.
- Historical exit and failure rule: grouped correction stopped at the goal atlas; ambiguity, a repeated non-goal position, a cycle, ten observations, or one full-turn pulse budget ended the feedback path and fell back to the then-bounded refinement path.
- Current boundary: beta.33 executes only Fast preparation from GB-024: canonical client size, maximum zoom, and vertical pitch clamping through balanced Shift Lock. It sends no arrow pulse or horizontal relative movement and performs no learned yaw matching.
- Compatibility: public camera-model enums and fields remain deserialize-only so existing files can be loaded and shown with replacement guidance. Any legacy camera-model task or preset is rejected before Roblox discovery, capture, focus, or input.
- Do not: restore this algorithm through a hidden compatibility path, treat a parsed legacy camera-model field as runtime permission, or make Fast preparation depend on yaw references.
- Evidence: beta.13 setup and manual Auto Align Deep Debug archives reviewed on 2026-07-23. Setup produced a clean 100% zero pose and 72-step return. The random runtime pose matched atlas position 37 at 88%; a rapid 35-pulse correction landed near position 8 instead of zero, after which the fixed rightward fallback needed 60 of 72 steps to recover the 96% goal. A later beta.14 dense setup/alignment pair located the runtime yaw at 99% fingerprint confidence with 16% remote separation but only 39% registered structure; its predicted 27 right pulses were within four pulses of the verified 31-pulse goal, while every six-pulse re-observation retained at least 97% fingerprint confidence and 7% remote separation.
- Protected by: `CameraPreparationExecutionPolicyTests.FastNoAlign_IsTheOnlyExecutablePreparationMode`, `CameraPreparationExecutionPolicyTests.CameraModel_StopsExecutionWithActionableGuidance`, `CameraPreparationExecutionPolicyTests.MacroPlanWithLegacyPresetTask_RemainsValidAndVisible`, `FastOnlyRunnerCompatibilityTests.LegacyExpeditionPreset_StopsBeforeRobloxDiscovery`, `FastOnlyRunnerCompatibilityTests.LegacyChallengePreset_StopsBeforeRobloxDiscovery`, and `FastOnlyRunnerCompatibilityTests.LegacyStagePreset_StopsBeforeRobloxDiscovery`.

### GB-016: Retired camera-model behavior — dense yaw learning

- Status: **Historical release behavior; retired in v1.3.0-beta.33**. The exact continuous-versus-pulsed rate was device-local, and beta.33 no longer learns, saves, or executes camera models.
- Historical entry: camera setup standardized the Roblox client, zoom, pitch, and Shift Lock state and captured a stable goal.
- Historical action: the former setup sampled signed fine-yaw positions, held Right Arrow while building regional fingerprints, proved a single circular period, returned through bounded re-observed arrow groups, and saved schema 4 only after independent sweep-release and pulse-probe returns to the fine-verified goal.
- Historical safety rule: the workflow did not infer yaw solely from elapsed hold time, assume equal opposing pulse counts were reversible, accept descriptor similarity as final verification, save a partial sweep, or leave a held input pressed after failure.
- Current boundary: no camera-model repository, setup surface, calibration sweep, yaw atlas, arrow-pulse executor, horizontal-yaw refinement, or learned-camera diagnostic payload participates in beta.33 runtime. Only Fast preparation from GB-024 is authored and executable.
- Compatibility: legacy camera-model fields remain parse-only and are preserved where required to show actionable migration guidance; they are never composed into runtime services.
- Do not: recreate camera-model folders during normal app-path initialization, copy legacy model/shortcut payloads into new Deep Debug archives, or reinterpret a legacy field as Fast.
- Evidence: eleven same-map beta.14 Deep Debug setups reviewed on 2026-07-23. All eight completed turns entered the saved fine neighborhood on their first turn, but only one produced a moving frame above the old near-exact structural gate; three additional attempts returned to a strong stationary zero after one transient moving-zero capture. A later eleven-run, multi-map beta.14 set contained seven successes and four retries: two recognized the loop but landed beyond the ±16-pixel fine window after key release, while two sent 12 right and 12 left calibration pulses yet remained at only 32–33% goal confidence. Successful siblings on the same scenes required 4–8 fine steps after release and as many as 14 after the pulse probe, confirming that both boundaries require atlas feedback rather than input-count reversal. Near-identical beta.15 success/failure controls reviewed on 2026-07-24 used the same 30 ms arrow setting and map: the success recognized 67 positions at the first 3.02-second seam, while the failure skipped that seam and saved 100 positions only after a second 6.03-second seam. A beta.16 failure repeated the same pattern with 138 positions over 6.05 seconds. Distributed retrospective comparisons independently identify the duplicated cycle in all supplied six-second traces. A later local-build setup reviewed on 2026-07-24 reached the goal by fine mouse correction from atlas bin `1/78`, but the former controller ignored that verified return and alternated between `1/78` and `77/78` for 30 arrow pulses before its round limit accepted the same goal.
- Protected by: `CoreModelTests.AppPaths_CreatesActiveFoldersWithoutRecreatingRetiredCameraFolders`, `CameraPreparationExecutionPolicyTests.FastNoAlign_IsTheOnlyExecutablePreparationMode`, `CameraPreparationExecutionPolicyTests.CameraModel_StopsExecutionWithActionableGuidance`, `CameraPreparationExecutionPolicyTests.MacroPlanWithLegacyPresetTask_RemainsValidAndVisible`, `FastOnlyRunnerCompatibilityTests.LegacyExpeditionPreset_StopsBeforeRobloxDiscovery`, `FastOnlyRunnerCompatibilityTests.LegacyChallengePreset_StopsBeforeRobloxDiscovery`, `FastOnlyRunnerCompatibilityTests.LegacyStagePreset_StopsBeforeRobloxDiscovery`, and legacy-schema/Deep-Debug compatibility tests.

### GB-017: Wait for fresh, stable, owner-proven UI actions

- Status: **Release retained** across Play, Lobby, Settings, Challenge, Expedition, Story, Raid, Event, saved-team, placement, and recovery navigation.
- Entry: a detector recognizes an owned interface while Roblox may still be loading or animating its panel into place.
- Observation rule: count completed detector observations, including a fresh caller-supplied initial frame, instead of expiring solely because one slow capture crosses the nominal soft wait. Continue only within the operation's separate hard deadline; this extension never increases its click, drag, keypress, parking, or recovery-attempt cap.
- Action rule: require the expected owner state across its configured stable-frame count. When that state owns a live action center, also require at least two consecutive action observations within three client pixels. A manifest or other static fallback coordinate is advisory only and cannot authorize input when the current frame does not prove the owning state.
- Retry rule: Challenge, Stage, and Expedition may send an unchanged verified owner/action no more than three times. After every input, clear prior stability and require fresh consecutive proof. While confirmation of that transition is pending, block duplicate and alternate input. Unclassified frames, stale coordinates, and a different unverified candidate do not reset the attempt budget; only a verified transition to another owned state does. Expedition recovery additionally stops after eighteen total action attempts even when it cycles through valid states.
- Exit: click the action from the latest stable observation, then verify the destination state normally.
- Do not: accept a single late Play frame, reuse action evidence from before input, let identical state labels alone authorize a moving detector-owned button, extend a maximum-attempt counter to fill a slow-device time budget, or treat a detector pack's static action coordinate as proof that the action's owning screen is present.
- Failure rule: if the state or action moves, reset the stability candidate and continue within the existing bounded navigation timeout. If the hard deadline or unchanged-owner input cap expires, send no further input and report the existing state-specific failure.
- Evidence: the Unit Teams opening sequence reviewed on 2026-07-24 retains the `Teams` state while its real scrollbar is still moving. Earlier field captures also establish vertically shifted Story/Raid party and Challenge dialog families whose live action locations differ.
- Protected by: `ObservationWaitBudgetTests.RequiredObservationsSurviveTheSoftDeadline`, `ObservationWaitBudgetTests.LongSoftTimeoutIsNotShortenedByTheHardDeadline`, `ObservationWaitBudgetTests.StaleManifestCoordinatesOnUnknownFramesDoNotAuthorizeAClick`, `ObservationWaitBudgetTests.TwoOwnedFramesAreRequiredAfterAnUnownedFrame`, `ObservationWaitBudgetTests.LosingTheOwnerResetsPendingCoordinateEvidence`, `ChallengeActionRetryTests.SlowActionProof_CannotExceedMaximumParkingAttempts`, `ChallengeNavigationInputGateTests.StaticScreen_StopsAfterThreeVerifiedInputAttempts`, `StageNavigationTransactionStateTests.StaticVerifiedActionStopsAfterThreeAttempts`, `StageNavigationTransactionStateTests.PendingSlowConfirmationBlocksAlternateNavigationInput`, `ExpeditionRecoveryTransitionLoopTests.CyclingStatesStillStopsAtTheOverallAttemptCap`, `StableNavigationActionTrackerTests`, and the Stage/Challenge/Expedition navigation suites.

### GB-018: Isolated Debug operations

- Status: **Product contract** built on the field-confirmed navigation states above.
- Navigation start: the user explicitly chooses either a verified lobby with Play closed or a verified post-match result/party state. An already-open unrelated selector is rejected instead of being treated as the requested start.
- Navigation end: enter the chosen Expedition, Challenge, Story, or Raid route and stop at stable prestart. Do not align the camera, load a team, place units, or click Start Game.
- Team start/end: begin with Units closed, open Units through the configured key, load Team 1–8 through the production scrollbar/action verifier, and close Units before completing.
- Fast no align: begin with Shift Lock off and a visible supported Roblox window. Standardize the client to 808 by 611, clamp maximum zoom, temporarily enable the configured Shift Lock key, use only vertical relative camera drags to clamp top-down pitch, then restore Shift Lock. Arrow pulses and horizontal relative movement are forbidden so yaw remains unchanged.
- Step semantics: a detection checkpoint may pause after a detector observation; an action checkpoint pauses before input. Previous/Next only review captured history. Step authorizes one pending live boundary, while Run resumes without additional gates.
- Ownership: every Debug tool uses the exclusive operation coordinator. When Deep Debug is enabled, the archive includes the selected tool/preset, step mode, ordered checkpoints, frames, detector traces, and resulting input events.
- Do not: interpret rewind as an attempt to reverse already-sent Roblox input, or maintain separate Debug-only click coordinates.
- Protected by: `CameraPosePreparationServiceTests`, `DebugCheckpointControllerTests`, the existing mode navigation suites, saved-team tests, Deep Debug archive tests, and both-theme Debug page snapshots.

### GB-019: Resource-refuel calibration and scheduled Utilities

- Status: **Field confirmed route with production Macro scheduling and Debug calibration**.
- Start A: the player is in the standard lobby pose with Areas closed. Start B: close the verified Roblox process, launch the saved private-server link, then wait for the same stable lobby. Scheduled start C: accept a verified Lobby or an owned shared-Play surface; close a verified Challenge selector and every remaining verified Play layer before pressing Areas, then select Lobby and its Spawn card and require stable Lobby with Areas closed before continuing. The Play key is open-only and is never used during this scheduled return.
- Areas route: press the configured **Toggle Areas** letter, require its complete five-action category rail with exactly one selected category and no extra lower action, then select **Expeditions** and click the freshly detected **Expeditions Hub** card frame. Decorative headers, bottom borders, and mutable page contents never authorize input.
- Blind movement: after the Hub click, wait for teleport completion, then perform the user-configured Gold Mine route (`W`, `A`, `W`) or Resource Drill route (`W`, `A`, `W`, `A`). Do not use image/color detection during this movement.
- Default routes: Gold Mine holds `W` for 3000 ms, `A` for 820 ms, then `W` for 2600 ms. Resource Drill holds `W` for 3000 ms, `A` for 750 ms, `W` for 1000 ms, then `A` for 1600 ms. The schema-5 upgrade applies those field-confirmed timings once to older persisted profiles; edits saved after that migration remain user-controlled.
- Interaction: press `E`, then begin visual verification. Require the expected Gold Mine or Resource Drill station accent, red Close, live two-dimensional orange **Add Fuel** control, both Building Stats bars, and Rewards frame structure. The fuel card may show Missing Fuel or fuel present, and **Claim Rewards** may independently be enabled or disabled; neither mutable content changes station ownership. Click the detected Add Fuel action, then require a quantity dialog with live **Max**, **Confirm**, and **Cancel** components plus the retained Stats and Rewards structure. Returned coordinates follow the observed controls or translated owned layout.
- Retry: if `E` does not expose the expected station panel, reopen Areas, teleport to Expeditions Hub, and replay the complete configured route. Bound retries to the saved Debug value.
- Between stations/end: use Areas again between Gold Mine and Resource Drill. A Debug test opens Play after the final station. A scheduled Utility never presses Play after refueling: it presses the configured Areas key directly from the station state, selects the detector-owned **Lobby** category, clicks the live **Spawn** card, and proves Lobby again before returning scheduler ownership. The configured Play key opens Play; it does not close Play and must not be retried as a close action.
- Schedule: Gold Mine, Resource Drill, and combined Utility tasks are eligible immediately on their first plan pass. After successful completion, persist the next eligible timestamp as completion time plus the configured 1â€“10,080 minute interval. While that timestamp is in the future, treat the Utility as complete for the current loop pass and allow lower-priority tasks to run. For a combined task, persist the first verified station before navigating to the second; Stop/Start or process recovery resumes only the remaining station. A failed station records no new station checkpoint or due time. Completing the cycle clears its partial checkpoint, and **Reset progress** clears both the checkpoint and due time.
- Ownership: use the same coordinator, focus/canonical-client checks, acknowledged clicks, held-key cleanup, checkpoint stepping, and Deep Debug tracing as other automation.
- Product boundary: Debug owns route timing/retry calibration and isolated tests. Macro Plan owns only the refuel target and interval; it calls the same Automation service with the locally calibrated settings. Share codes include the target and interval but exclude key bindings and calibration settings as account/device configuration.
- Evidence: a 419-frame passive capture reviewed on 2026-07-24 covers Lobby → Areas → Expeditions Hub → Gold Mine → Add Fuel → Areas → Expeditions Hub → Resource Drill → Add Fuel → Play. Physical movement/input timing is user described because the passive archive contains frames but no input events. A beta.41 Deep Debug run reviewed on 2026-07-29 proves the old completion path correctly recognized Play at frame 397, then pressed the open-only Play key and remained on Play. A separate 92-frame passive `lobby.zip` capture covers Areas opened from Lobby, the Lobby category, its Spawn card, the loading transition, and the returned Lobby; it contains no input events, so the exact physical clicks are user described while their destination states are visually confirmed. A second beta.41 Deep Debug failure reviewed on 2026-07-29 shows scheduled refuel opening Play after a successful station action, then failing every Areas attempt because Play remained open; this confirms that scheduled completion must enter Areas directly from the station state.
- Additional evidence: a beta.40 Deep Debug run reviewed on 2026-07-29 reached the current Gold Mine Missing Fuel panel on every bounded attempt, but the former detector returned `None` because it incorrectly required the unrelated Claim Rewards action to be green. Four same-day canonical passive sets cover Missing Fuel and fuel-present states for both stations with both reward-button states.
- Protected by: `ResourceRefuelDetectorTests`, `ResourceRefuelServiceTests`, `MacroSchedulerTests.UtilityRunsImmediatelyThenHandsOffUntilItsInterval`, `MacroSchedulerTests.RouteIntoUtility_RequiresLobby`, Utility model/share tests, Areas-binding persistence/conflict tests, Deep Debug sanitized-settings tests, and both-theme Debug/Settings/Utilities snapshots.

### GB-020: Select a high-saturation Expedition reward

- Status: **Field confirmed**.
- Entry: a live three-card Expedition reward chooser with a global blue/cyan wash, a thin progress bar, and three **Select Upgrade** actions.
- Action: identify the thin bright progress bar independently from the broader wash, require the repeated live action row, then click the first detected **Select Upgrade** action.
- Exit: the reward chooser closes and ordinary node monitoring resumes.
- Do not: reject the chooser merely because a color/HDR profile makes the full header band cyan, click a card from the overlay color alone, or wait for Roblox's automatic selection timer.
- Evidence: three consecutive beta.16 Deep Debug frames reviewed on 2026-07-24. The complete chooser remained visible for about 24 seconds with a reward score of zero and no macro input; Roblox eventually auto-selected the right card. The affected profile made all 55 rows of the old broad cyan search band pass, while the actual bright progress bar remained a distinct seven-row structure.
- Protected by: `DetectorPackGoldenTests.RewardDetector_IsRarityIndependentAcrossAllCapturedCardDatasets`, the complete cross-state corpus, and `Expedition_Reward_Select5`.

### GB-021: Recover a match that exceeds its possible runtime

- Status: **User-confirmed product contract** based on observed match ceilings.
- Entry: the macro has clicked Start Game and no Victory, Defeat, or root recovery state has completed the active match.
- Fifteen-wave limit: Story Acts 1-5, Story Mastery, every Regular Challenge, and Raid Acts 1-3 must reach Victory or Defeat within 12 minutes. This deliberately exceeds the observed two-to-eight-minute range so slow and debuff-heavy teams remain valid.
- Infinite rule: Story Infinite has no match-runtime watchdog because its valid duration is indeterminate.
- Expedition limit: when checkpoint extraction is enabled, a zero-boss target has a 10-minute limit; positive targets have 15 minutes per requested boss (`1 = 15`, `2 = 30`, `3 = 45`, and so on). An Expedition with extraction disabled has no target-derived watchdog.
- Failure rule: emit a recoverable timeout before any task progress is recorded. Save configured failure diagnostics, close only the verified Roblox process, relaunch the configured private server, reload the saved plan, and retry the same incomplete task. The existing ten-restarts-per-ten-minutes circuit breaker remains authoritative.
- Do not: count a watchdog timeout as Victory, Defeat, a defeat retry, or completed task progress; impose the 12-minute limit on Story Infinite; or restart without a configured private-server target.
- Protected by: `MatchRuntimePolicyTests`, `RecoveringMacroSchedulerTests.RuntimeFailure_RestartsRobloxAndRetriesTheIncompleteTask`, and the mode-monitor suites.

### GB-022: Dismiss rare Raid unit drops after placement

- Status: **Field confirmed** for Spirit City Acts 2 and 3.
- Entry: Start Game has been clicked and every configured after-start placement step has returned successfully. The monitor has then observed the ordinary unit hotbar, Unit Manager action, and Stage Info action in consecutive settled frames.
- Trigger: while no Victory or Defeat candidate is visible, the established gameplay HUD disappears in consecutive frames. The rare unit-drop presentation hides all three signals and requires a click anywhere to close; its unit, rarity, and artwork can vary.
- Action: click the bottom-right client resting area at `(783, 586)` through the acknowledged client-relative click path. If the HUD remains hidden, retry no more than once per second until gameplay or a terminal candidate returns.
- Scope: enable this behavior only for Raid Acts 2 and 3. Story, Challenge, Expedition, and Raid Act 1 do not own this unit-drop rule.
- Do not: inspect for the overlay before after-start placement completes, click continuously during placement playback, recognize one specific unit or shiny variant, or click after a Victory or Defeat candidate appears.
- Evidence: a five-frame beta.17 passive capture reviewed on 2026-07-25 shows the stable 8th Sword drop presentation for five seconds with the hotbar and both right-side gameplay actions absent.
- Protected by: `RaidDropDismissalTrackerTests` and `StageGameplayHudDetectorTests`.

### GB-023: Resume a private-server restart only from stable Lobby

- Status: **Product contract**.
- Entry: process-level recovery has closed the verified Roblox PID, launched the configured private-server URI, and discovered a different supported Roblox player PID.
- Readiness: standardize the new client to 808 by 611, then require three consecutive detector observations whose recovery state is exactly `lobby`.
- Intermediate rule: unknown, splash, teleport, loading, Play, selector, prestart, result, AFK, Disconnect, capture-error, and wrong-size observations reset Lobby stability and send no Roblox input.
- Exit: return control to the scheduler only after stable Lobby, reload the saved plan, and retry the same incomplete task.
- Failure rule: if the new process never reaches stable Lobby before the bounded recovery deadline, surface a session-level recovery failure. Do not consume Play-key attempts or route clicks during that wait.
- Protected by: `RobloxLobbyReadinessGateTests`.

### GB-024: Prepare the deterministic Fast placement pose

- Status: **User-confirmed product contract** with canonical map captures.
- Entry: a supported match has reached verified prestart. Roblox loads the map at its deterministic starting yaw; Fast is the only authorable and executable camera-preparation mode.
- Preparation: standardize the client to 808 by 611, clamp maximum zoom, temporarily enable the configured Shift Lock key, clamp straight-down pitch with vertical relative movement, then restore Shift Lock. Send no arrow pulse or horizontal relative movement.
- Reuse: prepare once per top-level macro operation and Roblox process. Reuse the pose across Repeat Stage and post-match Play handoffs, including a different mode or map, because those paths preserve zoom, pitch, and yaw.
- Invalidation: prepare again after any verified Lobby observation, after Roblox restarts into a new PID, or when a new top-level macro operation begins. A failed or canceled preparation is never cached.
- Match Steps authoring: use the exact embedded native 808 by 611 screenshot for the selected category, mode, map, act, or Story run. Every setup contains exactly one required **Start Game** step. Actions above it execute before the verified Start click; actions below it execute afterward. Start Game may move through the same drag/arrow ordering path but cannot be added, edited, duplicated, or removed. Current authoring has no Before Start / After Start selector, phase badge, or independently editable after-Start offset. Use explicit Delay actions to schedule later work. Only Place Unit actions own canonical coordinates or appear as map markers. Reconfigure Unit, Upgrade Unit, and Sell Unit select a stable placement identity such as `6a` or `6b`, then resolve its saved coordinate at runtime. Removing a Place Unit action removes every dependent Reconfigure Unit, Upgrade Unit, and Sell Unit action in the same autosaved timeline edit. Marker labels size for their content, choose collision-free horizontal and vertical offsets while remaining inside the map, and route connectors around every label without changing saved coordinates. Reject placement coordinates less than 7 client pixels apart or inside the fixed central hotbar/HUD rectangle. An Expedition setup may use each placement unit slot only once in the complete timeline. Existing files that violate either rule remain loadable and editable so the user can progressively move or remove offending rows; the remaining legacy duplicates do not block one cleanup edit, but an edit may never introduce another duplicate. Loading never silently deletes them. Match Steps grow the outer page rather than owning a nested vertical viewport. Map-wheel zoom is accepted only while the outer workspace is fully scrolled to its top; otherwise the wheel belongs to page scrolling.
- New-action ordering: append every newly authored Match Step at the bottom of the timeline below Start Game, whether it comes from **Add action** or a placement-map click. Preserve existing timelines unchanged; users may move an action above Start Game explicitly when it must run before the verified Start click.
- Unsafe placement coordinates: reject newly authored coordinates on the canonical client edge, including the default `(0,0)` point, as well as the fixed central hotbar/HUD rectangle. Keep older files loadable and editable, but skip each unsafe legacy row before sending its unit-selection key or coordinate input so a malformed row cannot open an unrelated HUD panel.
- Empty exact override: on a fallback-capable route in Step Mode, removing the final user-authored action leaves only structural Start Game and clears the exact override. The route immediately returns to its category setup for catalog, runtime, and export resolution. Do not clear a category setup or an exact route assigned to Recording Mode.
- Legacy migration: a setup without Start Game is interpreted through its saved phase fields exactly once. Preserve before-Start author order above a newly inserted sentinel. Sort after-Start rows by absolute offset with author order for ties, convert each positive offset delta into an explicit Delay action below Start Game, and clear the legacy offset fields. A setup with a sentinel derives parse-only legacy phase values solely from its position around that boundary. More than one Start Game step is invalid.
- Match Steps playback: validate each saved action independently. Log and skip a legacy central-HUD point or, for Expedition, a later placement action that reuses a unit slot; continue with the next valid action. Resolve both the physical Cancel Placement and Quick Placement keys before any Roblox input. For each contiguous placement batch, press Cancel Placement once, hold Quick Placement, tap a unit slot only when it differs from the preceding row, and send three complete clicks at each saved coordinate over exactly 50 milliseconds. Release Quick Placement and press Cancel Placement once after the batch. Delay, Reconfigure Unit, Upgrade Unit, and Sell Unit actions split placement batches and execute in timeline order. Sell always begins from a verified open selected-unit panel, taps the configured Sell Unit key, and gives the game one second to close the panel. If the panel remains stably verified, retry the key no more than twice; unknown or lost ownership sends no retry. Retire that placement identity only after stable panel closure, and stop with a recoverable UI failure if all three verified attempts are ignored. Do not image-detect the cyan Quick Placement title or use a timed drag approach. Placement keys start independently configurable and block only a plan or test that resolves at least one Step Mode setup with placement actions; the Sell Unit key is required only for a timeline that contains Sell. Recording Mode and a setup without placement actions remain exempt.
- Placement attempt: the initial placement batch is total attempt one. In a separate author-order verification pass, click each saved coordinate once, park the pointer because a hover card can cover the red Close control, and require the selected-unit panel in two consecutive canonical frames. A setup may configure one through eight total attempts and defaults to one. Each additional attempt re-places and rechecks only the failed row; it never replays already confirmed rows or the rest of the batch. The scoped hold must release Quick Placement and every mouse button on success, cancellation, or failure.
- Placement miss: if selected-unit proof is still absent after the configured total attempts, log and skip that row, send no targeting or Auto Upgrade input, do not invoke the successful-placement callback, and continue with the next configured step. Expedition prestart reporting counts only callbacks from normally confirmed rows and reports that value against the configured row count; it never labels skipped rows as sent placements. An empty Expedition hotbar slot is not accepted as placement proof because hover and scene changes can produce false occupancy transitions. This bounded local miss is not a runtime/session failure and cannot request private-server recovery. After ordinary positive panel proof, tap the configured Change Unit Targeting key zero through eight times for First, Last, Closest, Strongest, Boss, Weakest, Shielded, Fastest, or None, then tap Auto Upgrade Unit zero through six times for Off or Priority 1 through Priority 6. Newly authored steps default to Priority 1; missing legacy values remain Off. After every ordinarily confirmed unit, click the shared safe idle point up to eight bounded times until two consecutive frames prove the selected-unit panel closed. Only then may playback advance to the next placement, so an open panel cannot cover its coordinate.
- Authoring defaults: Match settings persist one Targeting priority and one Auto Upgrade priority for future Place actions. Map clicks and newly added Place actions inherit both values; applying new defaults never changes an existing action. A setup payload without those fields uses Target First and Auto Priority 1. A missing legacy per-step Auto Upgrade value remains Off.
- Placement proof: after a candidate placement click, accept the unit as selected only when the fixed lower-left panel exposes both its red Close control and initial blue **Priority / First** control. The dark panel body is supporting evidence only. An ordinary unit hover/info card is not placement proof, and the final configured targeting priority is applied by key taps without image-reading its label. Final cleanup uses the red Close control plus dark panel body as a separate panel-presence signal, because a configured targeting key may legitimately move the blue highlight away from **First** before cleanup.
- Advanced proof controls: placement configuration, reconfiguration, and Upgrade readiness are independently configurable. Disabling placement proof allows targeting and Auto Upgrade input after the coordinate click; if the requested placement state is already Target First and Auto Off, skip that coordinate click and all follow-up input entirely. Disabling reconfiguration proof allows its targeting and Auto Upgrade taps after the coordinate click without panel evidence. Sell never permits a proof opt-out. Legacy setup files that contain only the former combined proof setting use that saved value for both new responsibilities until the user saves explicit choices.
- Compatibility: resolve an exact Fast setup before its category fallback. The Expeditions category covers Expedition maps 1-3. Each Story-map category covers that map's Acts 1-5, Infinite, Mastery, and Challenge. Raid acts have no category fallback. A category from another map or mode remains incompatible. Public legacy files with a Camera-model strategy or no strategy field remain parseable so users can identify and replace them; the app does not reinterpret or execute them. Validation rejects that retired mode before Roblox discovery, capture, focus, or input. New authoring, sharing, and runtime composition emit only Fast.
- Evidence: eleven privacy-reviewed canonical selected-unit-panel frames extracted from a passive Deep Debug capture on 2026-07-25 cover five shared Story/Challenge maps, all three Spirit City Raid acts, and all three Expedition maps. Field playback showed that Quick Placement title geometry could vary independently of whether the slot/placement transaction should proceed, so beta.37 removed that preliminary image gate. A user-performed 2026-07-28 recording confirmed that Quick Placement preserves the consecutive unit selection and that three direct coordinate clicks can be completed in 50 milliseconds before a separate selected-unit verification pass. A beta.50 Deep Debug Story run reviewed on 2026-07-31 proves Roblox remained focused and received a complete 70-millisecond `X` down/up input while the correctly detected selected-unit panel remained unchanged; the prior one-attempt transaction restarted Roblox after only two post-input panel observations.
- Protected by: `CameraPosePreparationServiceTests.PrepareWithoutYaw_ClampsPoseWithoutYawInput`, `FastNoAlignPreparationSessionTests.EnsurePrepared_ReusesPoseUntilLobbyIsObserved`, `FastNoAlignPreparationSessionTests.EnsurePrepared_DoesNotReuseAcrossRobloxProcesses`, `FastNoAlignPreparationSessionTests.FailedPreparation_IsNeverCached`, `CameraPreparationExecutionPolicyTests.FastNoAlign_IsTheOnlyExecutablePreparationMode`, `FastOnlyRunnerCompatibilityTests.LegacyExpeditionPreset_StopsBeforeRobloxDiscovery`, `FastOnlyRunnerCompatibilityTests.LegacyChallengePreset_StopsBeforeRobloxDiscovery`, `FastOnlyRunnerCompatibilityTests.LegacyStagePreset_StopsBeforeRobloxDiscovery`, `PlacementTimelinePolicyTests`, `MatchStepModelTests`, `MatchStepShareTests`, `PlacementPlaybackAttemptTests.SellStep_IgnoredFirstPress_RetriesFromVerifiedPanel`, `PlacementPlaybackAttemptTests.SellStep_AllPressesIgnored_StopsAfterThreeAttempts`, `PlacementServiceObservationBoundaryTests`, placement-safety and legacy-compatibility tests, compact Match Steps reachability checks, and both-theme Placement Setup snapshots.

### GB-025: Independently normalize UI Scale and required settings at startup

- Status: **Field confirmed**.
- Entry: the Roblox client is standardized to 808 by 611, Settings is closed, and the Macro starts from the fully loaded Lobby. UI Scale correction and required game-settings correction have independent controls. If either responsibility is enabled, temporarily enable the configured Shift Lock and clamp pitch to the repeatable straight-down limit. Do not zoom or change yaw; restore Shift Lock before Settings input. The standalone Debug UI Scale action may begin from Lobby, gameplay, prestart, or another loaded state.
- Disabled checks: disabling UI Scale correction skips all scale measurement and scale input; the scene-sensitive Lobby side-rail estimate is not used. Disabling required game-settings correction skips all profile-page and toggle actions. Disabling both sends no camera or Settings input and retains only the stable-Lobby gate.
- UI-scale action: after the pitch-only preparation completes, detect the opaque white Roblox Settings gear at its stable top-bar position. The gear center is `(232, 34)` without a voice control and `(276, 34)` when the top bar includes a microphone or headset control; Anime Expeditions UI Scale does not move either Roblox-owned position. Match the gear itself at both offsets without inspecting or depending on the voice control. Accept the field-observed normal and high-contrast white gear strokes while still requiring the gear's spoke/center structure and rejecting the adjacent ellipsis and door glyphs. Require two stable action observations, then mouse-click the detected gear through acknowledged client-relative motion. Require the gear's filled selected form together with two settled Settings-panel observations before using the panel. When scale is not canonical, detect the selected Settings page from the live capture, derive the scale-aware Misc tab action, require stable action geometry, and mouse-click it when needed. Verify Misc, require two consecutive observations of the scale-aware UI Scale text input at unchanged action geometry, mouse-click its right interior once, and wait 2.5 seconds for Roblox to establish text focus before typing. The cyan ring belongs to accessibility selection and is not a text-focus signal or an acceptance requirement. Clear the value, enter an initial `1.00`, and apply. Measure two consecutive settled rendered-panel scales; if the result is not canonical, redetect and click the input again, then enter `applied value / observed rendered scale`, rounded to two decimals and clamped to 0.80–1.20. Repeat boundedly until the rendered panel independently verifies canonical scale. Numeric `1.00` is not assumed to render identically on every device.
- Intermediate: require two stable observations of the same filled Settings gear and click that gear again to close Settings. Do not click the red Close control inside the Settings panel. Verify both that the panel is absent and that the same top-bar gear has returned to its outline form. Enabled Macro startup then requires three consecutive clean Lobby detections. If required game-settings correction is also enabled, reopen Settings through the same detector-owned gear action and correct the required profile. This clears the Settings search/filter before coordinate-based page navigation. The required-settings responsibility never changes UI Scale; when UI Scale correction is disabled, its direct panel measurement must already be within the inclusive rendered range `0.98–1.02`. The standalone UI Scale Debug action ends after panel closure and does not require Lobby.
- Required profile: Gameplay enables Auto Skip Waves and disables Auto Vote Start, Show Match End Rewards, Display Pinned Quests, Select Unit on Placement, Display Path Visualizers, Auto Retry, and Auto Next. Graphics disables Camera Shake, Depth of Field, Night Time, and Event Theme while enabling Low Detail. Units disables other/own Unit VFX, Ability Effects, Unit Aura, Trait Aura, Damage Indicators, and Auto-Upgrade Placed Units while enabling Strict Phantom Placement, Prioritize Phantom Placement, and Auto Abilities on Placement. The macro owns per-unit Auto Upgrade after each verified placement. Misc disables the update log on login and enables Auto Sprint. Unlisted controls have no required state and remain untouched. Toggle evidence may settle within the reviewed two-pixel vertical neighborhood, but the action remains at the canonical control center. Before a toggle action, require two consecutive recognized observations with the same state and action coordinate. A page that appears before its controls finish rendering remains observation-pending until that proof succeeds or the bounded hard deadline expires; unknown controls never receive input. A recognized control that ignores input may be clicked at most twice.
- Units scroll: require two consecutive observations of identical live blue scrollbar-thumb geometry before deciding that it is already clamped or dragging it to a verified top or bottom boundary. A missing or changing thumb remains observation-pending within the same hard bound and receives no drag. A recognized stable thumb may be dragged at most twice for each boundary.
- Exit: close Settings and require three consecutive Lobby detections again before the scheduler may choose its first task.
- Do not: use accessibility navigation to open, close, or traverse Settings; detect the microphone/headset control; scale the Roblox-owned gear coordinates from Anime Expeditions UI Scale; click the red panel Close control; assume only one top-bar offset; zoom or rotate yaw during the protective pitch clamp; estimate UI Scale from Lobby scenery; assume numeric `1.00` is universal; click an unverified page/input/toggle; infer a toggle from label text or a custom font; act on the opening animation; click an unknown control; or begin task navigation from a non-Lobby screen. A disabled responsibility must not send its owned input.
- Failure rule: use bounded gear, panel, page, toggle, scale, and scrollbar verification. Never click an uncertain control or begin match navigation. A recognized gear or control that ignores input, an unstable panel/Lobby, or a lost/resized Roblox window is a recoverable UI/session failure. An unrecognized Settings gear or another unknown control is a hard compatibility failure and must not trigger a deterministic restart loop. If the initial preflight never completed, recovery may retry only a recoverable failure; after it succeeds, do not rerun it during the same macro operation.
- Evidence: two pixel-identical 287-frame diagnostic captures reviewed as timestamped contact sheets on 2026-07-25 cover UI Scale 0.80/1.00/1.20, all required pages, and both Units scrollbar boundaries. A 41-capture field corpus reviewed on 2026-07-27 covers the Misc page and UI Scale input at every 0.01 step from rendered scale 0.80 through 1.20; it confirms that accessibility traversal inside Settings varies across devices. Two beta.31 Macro traces reviewed on 2026-07-27 show Backslash, Right, and Enter being injected twice while accessibility navigation never activated, Settings remained closed through every observation, and runtime recovery repeated the deterministic failure. Seven additional field captures from the same day cover the opaque gear under moving worlds with and without a microphone/headset control, establishing the fixed `(232, 34)` and `(276, 34)` action centers and the selected gear used to close the panel. Later same-day startup archives preserve brighter, higher-contrast forms at both offsets; the former narrow outline-percentage ceiling rejected those real gears before any Settings click. A beta.31 Debug UI Scale archive shows the detected click selecting the numeric input after accessibility navigation was disabled; the macro incorrectly waited for the now-inapplicable cyan accessibility-selection ring through 35 post-click observations and stopped before typing. Six same-device beta.31 startup traces used the same actual in-game value `1.00`, but the Lobby-scene estimator fluctuated from `1.01` through `1.16`; direct Settings-panel measurement in the enabled trace reported `1.0002` at 93.8% confidence. The Lobby estimate is therefore not a valid acceptance or correction signal. A beta.20 Debug UI Scale trace contains 19 fully loaded Lobby frames whose scale-dependent recovery detector returned no Lobby state; the old pre-scale gate therefore sent zero input and failed before opening Settings. A current-update capture adds **Event Theme Enabled** to Graphics and moves the Misc controls. A red Event-theme Lobby trace proves that its bright railing can resemble the Settings Close component, but it cannot pass without the complete Settings page rail.
- Protected by: `CameraPosePreparationServiceTests.PreparePitchOnly_PreservesZoomAndYaw`, `MacroStartupPreflightServiceTests.UiScaleDebug_NonLobbyStartDoesNotRequireLobby`, `MacroStartupPreflightServiceTests.GameSettingsDebug_PreparesPitchBeforeSettingsGear`, `MacroStartupPreflightServiceTests.VoiceLayout_UsesSameDetectedGearToOpenAndClose`, `MacroStartupPreflightServiceTests.HighContrastVoiceLayout_StillOpensAndClosesSettings`, `MacroStartupPreflightServiceTests.HighContrastNoVoiceLayout_StillOpensAndClosesSettings`, `MacroStartupPreflightServiceTests.MissingGear_IsAHardCompatibilityFailure`, `MacroStartupPreflightServiceTests.RecognizedGearClickWithoutPanelReportsOpenPhase`, `MacroStartupPreflightServiceTests.NonLobbyStart_NormalizesUiScaleBeforeLobbyGate`, `MacroStartupPreflightServiceTests.DisabledUiScaleCheck_SkipsScaleMeasurementAndSettingsInput`, `MacroStartupPreflightServiceTests.UiScaleOnly_CalibratesWithoutCheckingGameSettings`, `MacroStartupPreflightServiceTests.GameSettingsOnly_CorrectsProfileWithoutChangingUiScale`, `MacroStartupPreflightServiceTests.DeviceDependentScale_UsesRenderedFeedback`, `MacroStartupPreflightServiceTests.EventThemeLobby_IsAcceptedAsCleanBeforeSettingsNormalization`, `MacroStartupPreflightServiceTests.ToggleWaitsForStableControlPastSoftDeadline`, `MacroStartupPreflightServiceTests.ScrollbarWaitsForStableThumbPastSoftDeadline`, `RobloxSettingsButtonDetectorTests.HighContrastVoiceGear_RemainsClosed`, `RobloxSettingsButtonDetectorTests.HighContrastNoVoiceGear_RemainsClosed`, `RobloxSettingsButtonDetectorTests.HighContrastSelectedGear_RemainsSelected`, `RobloxSettingsButtonDetectorTests.HighContrastEllipsisAndDoorWithoutGear_AreRejected`, the complete cross-state corpus, and both-theme Dashboard/Settings snapshots.

### GB-026: Recover owned Roblox UI failures without recording progress

- Status: **Product safety contract**.
- Entry: a Macro plan owns Roblox and a verified runtime/session operation cannot finish safely: the window is missing, changed, unfocusable, or incorrectly sized; a known panel, button, or scrollbar will not settle or acknowledge input; or a verified navigation transition times out.
- Action: stop task input, save the configured automatic diagnostic, close only the verified Roblox player process, and reopen the configured private server through the registered `roblox://` protocol.
- Resume: require a new Roblox PID and three stable Lobby frames, reload the saved plan, and retry the same task. UI Scale/game-settings preflight is operation-scoped and is not repeated after a successful startup check. The failed attempt must not increment victories, defeats, runtime target, or completion.
- Hard boundaries: cancellation, invalid plan/setup/model data, unsupported detector layout, an invalid Play key binding, malformed private-server link, manual-playback timing drift, internal state-machine invariant, or no configured restart target never authorizes process restart. Stop after ten restarts in ten minutes.
- Notification: recoverable attempts may create diagnostics and recovery log entries, but must not claim the Macro stopped unexpectedly while it is continuing.
- Protected by: `RobloxRuntimeRecoveryPolicyTests`, `RecoveringMacroSchedulerTests`, Team/UI action regressions, and lobby-readiness tests.

### GB-027: Enter and repeat the Villain Invasion Event

- Status: **Field confirmed** for Acts 1–4.
- Entry: a verified Lobby with Play, Areas, Units, Settings, and the Event panel closed. Event routes have no Play-interface tile.
- Navigation: click the detected Lobby **Events** action. Accounts may open another Event by default. If the stable Event catalog shows the wide cyan selected tab and chevron for Beginner's Path plus the thin red, unselected Villain Invasion rail, click the Villain Invasion card once at its detected action and then separately require the wide red selected tab, white chevron, and live Event Gamemode button. That button proof includes its isolated rectangular borders and neutral label; both narrow border bands accept their strongest horizontal row because Windows can render the same border one pixel thinner. The Act carousel's red rail can cross the same fixed action region but must never authorize the click. The decorative red Events header is not action ownership and may still be loading. If Villain Invasion is already selected in either observed row position, continue without the extra card click. Click its game-mode action and verify the generic act-selector structure before choosing an act. Event Act selector and Act detail may use the retained wide selected Villain Invasion tab at either observed row as Event context; a thin rail is insufficient. Selector ownership still requires its full-width rail and opaque subtitle, while detail ownership still requires the live Stage detail and **Select Stage** structure. Classification checks the shifted selector before retained Event-home evidence. Locate the configured act from its live colored emblem: purple for Act 1, green for Act 2, cyan for Act 3, and yellow for Act 4. Require that target to settle across consecutive frames before clicking its containing card. Scroll the carousel first for Acts 3 and 4; never rely on one fixed card coordinate. Then click the detected **Select Stage**, verify the preview, click its live Start action, and wait for verified prestart.
- Transition ownership: Event → Story, Challenge, Expedition, or Raid may use the post-match Play interface. Normal mode → Event and Event → Event must return to verified Lobby first because the Event route is absent from Play. If an unavailable Challenge hands scheduling control back from the shared selector, Event first backs through the shared selector and retained Challenge party with stable visual verification; only after Play is closed may it use the match-owned Back-to-Lobby control.
- Selector heading ownership: the Act Selection score requires a compact red heading run followed by its neutral subtitle inside the reviewed vertical spacing, plus the full-width selector rail. Broad title-region color alone is insufficient; multi-line Villain artwork and Roblox performance-overlay text must not turn Event Home into Act Selection.
- Preparation: use Fast no align at the first prestart. Act 1 Angle 1 then holds `W` for 750 ms, `D` for 750 ms, and `W` for 750 ms. Act 1 Angle 2 holds `W` for 750 ms, `D` for 750 ms, and `W` for 2100 ms to expose more of the front path. Act 2 holds `A` for 75 ms and `W` for 2000 ms. Acts 3 and 4 need no spawn movement. Trust these bounded movements and begin state detection again only after they finish.
- Repeat rule: Repeat Stage preserves player location and the Fast-prepared zoom/pitch pose, so do not repeat the act-specific spawn movement, Fast no align preparation, or an unchanged saved-team load on a verified Event Repeat Stage handoff. Act 1 Angle 1 and Angle 2 are distinct routes; switching between them must return to verified Lobby before navigation so the alternate spawn movement runs from the canonical spawn.
- Victory actions: Acts with an unlocked following act can add **Next Stage** before **Repeat Stage**; the final unlocked act omits it. Locate the yellow **Repeat Stage** action by its owned visual component instead of using a fixed action index.
- Runtime rule: Acts 1–3 are 15-wave matches with a 12-minute terminal deadline. Act 4 is a 25-wave match with a 17-minute terminal deadline. If no Victory or Defeat appears by the route-specific deadline after Start, record diagnostics and raise a recoverable session failure so configured private-server recovery can relaunch Roblox and retry the same incomplete task without progress.
- Do not: search Play for Event, send act movement more than once per initial stage load, classify Event route pages from labels or character artwork, or treat a thin unselected card rail as selected.
- Evidence: reviewed 808 by 611 captures from 2026-07-25 cover the Lobby Event button with and without its notification marker, Villain Invasion home, act selector, Acts 1–3 detail selection, preview, prestart, Victory both with and without **Next Stage**, Defeat, and three Fast no align placement views. A 77-frame manually dragged carousel sequence preserves the moving act-card positions and stable colored emblems. Additional 2026-07-26 captures cover the horizontally scrolled Act 4 selector, Act 4 detail, its no-movement Fast no align placement view, and its final Victory action rail. Paired three-frame 2026-07-27 captures reproduce Beginner's Path selected by default and Villain Invasion selected after one click. Their derived fixtures preserve only the colored tab geometry, white selection chevron, and owned action while redacting unrelated artwork. Additional same-day selected-home archives confirm Villain Invasion in either the first or second card row. A reviewed 17-frame canonical capture records the alternate Act 1 approach after the final 2100 ms `W` movement. Physical movement timing, Act 4's 25-wave/17-minute limit, and Repeat Stage preservation are user-confirmed behavior.
- Protected by: `EventScreenDetectorTests.EventCatalog_LocatesVillainInvasionCard`, `EventScreenDetectorTests.SelectedVillainEvent_UsesGameModeAction`, `EventScreenDetectorTests.ShiftedActSelector_PrecedesRetainedEventHomeEvidence`, `EventScreenDetectorTests.EventHome_RejectsActCarouselWithoutSelectorHeading`, `EventScreenDetectorTests.EventHome_RequiresSelectedVillainChevron`, `EventScreenDetectorTests.EventHome_DoesNotWaitForDecorativeHeader`, `EventScreenDetectorTests.ActEmblems_MapTheirLiveCards`, `EventScreenDetectorTests.ActEmblems_RejectCardsOutsideTheVisibleCarousel`, `EventPresetTests.ActOneAngleTwo_UsesTheLongerFinalMovement`, `MacroSchedulerTests.EventActOneAngleChange_ReturnsToLobby`, `EventRunPolicyTests`, Event placement compatibility checks, current cross-state detector tests, and the Event runner's stable verified navigation/terminal boundaries.

### GB-028: Begin a Macro plan from a fresh private-server Lobby

- Status: **Field confirmed** and **product safety contract**.
- Default entry: the user starts a top-level Macro plan with **Restart Roblox at Macro start** enabled and a valid private-server link configured.
- Persistence: changing either private-server restart checkbox writes that choice immediately through the serialized settings store. Page navigation may reload only the saved value and must not restore the previous toggle state. A failed write restores the last saved state and reports the failure beside the reconnect controls.
- Action: discover the supported Roblox player window. If one is open, close only its verified Roblox PID. Pass the normalized private-server `roblox://` URI to Windows whether or not a process was open.
- Startup readiness: discover a new supported Roblox PID and standardize its client to 808 by 611. Before the operation-scoped preflight, accept three consecutive exact Lobby observations or three consecutive unclassified observations whose raw Lobby evidence is within five percentage points of the configured Lobby threshold and at least twenty percentage points stronger than every competing state. This startup-only fallback proves that loading has reached a stable Lobby-shaped game view even when noncanonical UI Scale keeps strict Lobby classification just below threshold.
- Handoff: startup readiness authorizes only the configured UI Scale/game-settings preflight. If UI Scale correction is enabled, the preflight may now open Settings and normalize it before requiring three strict clean-Lobby observations. No task or route input may begin until that strict post-preflight Lobby verification succeeds.
- Recovery boundary: when process recovery is configured, a recoverable failure before startup preflight completes may relaunch through the same startup-only readiness so UI Scale correction remains reachable. After preflight succeeds, every process restart retains GB-023's strict three-Lobby-frame readiness. Never use the startup-only near-threshold fallback to resume an incomplete task.
- Frequency: perform this reset exactly once for each user-started Macro plan. Do not repeat it between tasks, loops, ordinary Lobby returns, or after the scheduler's separately bounded runtime-recovery restart.
- Disabled rule: when the toggle is off, retain the existing stable-Lobby startup requirement and never infer permission to close Roblox.
- Failure rule: enabling the option requires a valid private-server link before the operation starts. A failed launch, ambiguous/recognized non-Lobby view, or bounded startup-readiness timeout stops safely without Settings or route input. Preflight retains its own bounded Settings and strict-Lobby failure rules.
- Privacy: persist the private-server link only through the existing DPAPI-protected field. Deep Debug records only whether startup restart was enabled and whether a link was configured; it never records the link.
- Evidence: a beta.31 Deep Debug session reviewed on 2026-07-27 reached a stable 1.20-scale Lobby after restart, but its Lobby evidence remained `0.777–0.782` against the normal `0.800` threshold while the strongest competing state remained `0.465–0.471`. The old strict preflight gate therefore sent no input and made the enabled UI Scale correction unreachable.
- Protected by: `AppSettingsStoreTests.LegacySettings_DefaultStartupChecksToEnabled`, `RobloxStartupReadinessGateTests.Scale120FieldScores_ReachPreflightWithoutStrictLobbyClassification`, `RobloxStartupReadinessGateTests.RecognizedNonLobbyState_ResetsStartupStability`, `RecoveringMacroSchedulerTests.StartupRestart_RunsOnceBeforePreflightAndTask`, `RecoveringMacroSchedulerTests.StartupUiFailure_RestartsAndRepeatsPreflightBeforeTask`, `RecoveringMacroSchedulerTests.StartupRestart_WithoutPreparationHookRetainsStrictLobbyReadiness`, and `DeepDebugSessionTests.SuccessfulSessionArchivesFramesEventsSanitizedSettingsAndReferencedModels`.

### GB-029: Replay a manually recorded route from verified prestart

- Status: **Product safety contract**.
- Entry: the exact Fast route has a valid local manual recording, the client is 808 by 611, and Fast zoom/pitch, team, and deterministic route preparation has completed. First route entry always reaches a positively verified prestart. Repeat Stage also verifies prestart by default; an explicitly enabled advanced setup may instead trust the verified terminal and live Repeat Stage action, then wait its configured playback-start delay.
- Recording boundary: buffer physical keyboard and mouse hook observations on one timeline. Order them by the wrap-safe native Windows hook timestamp and retain callback sequence only inside the same native millisecond. If a button or wheel coordinate differs from the ordered path by one pixel, persist an explicit mouse move to the action coordinate at that offset; reject a larger gap before saving.
- Action: give the recording exclusive Roblox input ownership. Establish its saved initial client-relative pointer through acknowledged motion, then run one monotonic clock on a dedicated non-thread-pool timing worker and replay physical keyboard scan codes, mouse movement and buttons, and wheel events at their absolute saved offsets. Timing-critical window, client-geometry, focus, and pointer checks use direct uninstrumented Win32 probes rather than synchronous diagnostic wrappers. Before and after each injected event, measure the difference between total elapsed playback time and that event's recorded absolute offset. Signed drift within +/- 50 milliseconds is the quality target. Drift beyond that target continues on the unchanged absolute timeline, and playback stops only when drift reaches +/- 2,000 milliseconds. Immediately before every mouse button or wheel event, read the real client-relative cursor and require it to be within one pixel on each axis of the recorded position. The sequence itself owns the Start Game click.
- Intermediate: do not capture, detect, repeat Fast preparation, place units, or send any other workflow input while playback owns Roblox.
- Exit: after every recorded event is sent and all held inputs are released, resume the mode's normal bounded Victory/Defeat monitoring. Repeat Stage may replay the same recording after either fresh prestart proof or the explicit advanced delay-only policy; Challenge retries must not add their legacy fixed transition delay on top of that configured advanced delay.
- Do not: skip first-entry route/prestart ownership, bypass Repeat Stage proof without the explicit advanced opt-out, combine a manual recording with ordinary placement steps, teleport or correct the pointer at click time, accept a larger logical-versus-real cursor mismatch, shift later event deadlines to absorb timing drift, continue at or beyond the 2,000-millisecond hard stop, include raw input events in Deep Debug, or include any manual recording or recording reference in a share bundle.
- Failure rule: a native capture timestamp outside the operation or a button/wheel coordinate not reconciled by the ordered pointer path stops recording before save and asks the user to record again. During playback, cancellation, focus/window/geometry change, real cursor more than one pixel away on either axis, invalid recording data, input-send failure, or timing drift reaching +/- 2,000 milliseconds stops playback and releases every held key and mouse button. A hard-stop timing miss reports its signed early/late drift, event boundary, and whether input had already been sent, then stops locally; it is not a Roblox session timeout and cannot request private-server restart. A failed pointer proof sends no corrective motion and does not inject the pending button or wheel event. The incomplete attempt records no task progress. A per-setup impossibility threshold from 1 through 180 minutes may replace the route's default terminal deadline; zero preserves that default.
- Evidence: two beta.34 test playbacks reviewed on 2026-07-27 stopped before the same eleventh event with only 0.998 and 1.646 milliseconds of timing drift. The pending right-button action retained an earlier native pointer coordinate while its callback offset placed it after two newer mouse moves, producing a deterministic 6-by-8-pixel path discontinuity. Six of the recording's eight pointer actions had the same delayed-callback pattern, up to 53 pixels. A later beta.35 preview recording was natively ordered but stopped before event 324: its final move targeted `(431,180)` while right-button up targeted `(431,181)`. The successful control recording kept both coordinates identical. Persisting the one-pixel action anchor prevents Windows' separate one-pixel absolute-motion rounding from turning that valid capture variance into a two-pixel playback failure. Two beta.37 Macro sessions reviewed on 2026-07-28 completed repeated Victory-to-Repeat-Stage handoffs with successful playback drift of roughly 11 through 38 milliseconds, but seven other attempts missed the prior +/- 50-millisecond hard limit by roughly 52 through 117 milliseconds. Beta.38 gave those local scheduling misses a typed non-recoverable timing exception so they could no longer consume process recovery. The current policy retains +/- 50 milliseconds as the quality target while allowing the absolute timeline to catch up until the +/- 2,000-millisecond hard stop. A separate final trace lost Roblox foreground focus and remains an immediate safety stop.
- Privacy: recordings may contain typed secrets and remain device-local. Fast share export refuses a plan while any resolved Placement Setup references a manual recording; both schema 1 and schema 2 are recording-free.
- Protected by: `ManualInputCaptureTimelineTests`, `ManualInputWindowsTests.PlaybackPointerProof_AllowsOnlyOnePixelCoordinateVariance`, `ManualInputWindowsTests.PlaybackEngine_UsesAbsoluteMicrosecondDeadlines`, `ManualInputWindowsTests.PlaybackEngine_ReleasesHeldInputsOnCancellation`, `ManualInputWindowsTests.PlaybackEngine_AllowsSignedDriftBelowTwoSecondHardStop`, `ManualInputWindowsTests.PlaybackEngine_RejectsSignedTimingOffsetAtTwoSecondHardStopBeforeSend`, `ManualInputWindowsTests.PlaybackEngine_RejectsSignedTimingOffsetAtTwoSecondHardStopAfterSend`, `ManualInputRecordingTests`, `ManualInputRouteServiceTests`, `ManualPlaybackRunnerStartPolicyTests`, `PlacementManualRecordingTests`, `MatchRuntimePolicyTests`, `FastNoAlignShareTests`, and the Recordings/Placement Setup dark-and-light snapshots.

### GB-030: Count completed observations without relaxing input safety

- Status: **Field confirmed** and **product safety contract**.
- Entry: any owned UI wait—including Play, Lobby, Settings, Team, placement, Stage, Challenge, Event, Expedition, Continue, preview, prestart, terminal, and recovery—has a fresh detector result or begins capturing fresh observations.
- Action: count a valid caller-supplied first result, then allow enough fresh detector completions to satisfy the normal stability count even when one capture/detector pass takes longer than the nominal soft wait. A slow observation may extend the soft window only inside the separate hard deadline.
- Exit: act only after the unchanged owner state and live action geometry meet their normal consecutive-observation requirements.
- Attempt boundary: observation-aware waiting never increases a workflow's existing input cap. Challenge, Stage, and Expedition retain three attempts for one unchanged verified owner/action; Expedition recovery retains its eighteen-attempt overall cap. After input, stability resets and must be reacquired. Pending confirmation blocks alternate input, and only a verified transition resets an owner attempt budget.
- Do not: discard the caller's valid first observation, measure only wall-clock time while expensive observations are in flight, lower detector confidence, accept a lone transient frame, reuse pre-input stability, turn time into extra clicks, or authorize input from a static fallback coordinate.
- Failure rule: retain a separate bounded hard deadline and cancellation. If the required completed observations still do not agree, or the unchanged-owner attempt cap is exhausted, send no input and report the original state-specific failure.
- Evidence: five beta.28 diagnostic archives reviewed on 2026-07-27 showed valid Play, Lobby, and Continue observations taking roughly 2–6 seconds each; the old 3-second/two-sample and 12-second/three-sample wall-clock windows expired between valid observations.
- Protected by: `ObservationWaitBudgetTests.RequiredObservationsSurviveTheSoftDeadline`, `ObservationWaitBudgetTests.LongSoftTimeoutIsNotShortenedByTheHardDeadline`, `ObservationWaitBudgetTests.ExtendedSoftTimeoutRetainsABoundedObservationGrace`, `ObservationWaitBudgetTests.SlowContinueActionGetsItsSecondStableObservation`, `ObservationWaitBudgetTests.StaleManifestCoordinatesOnUnknownFramesDoNotAuthorizeAClick`, `ObservationWaitBudgetTests.InitialOwnedFrameCountsTowardStableActionProof`, `PlacementServiceObservationBoundaryTests.SelectedUnitProof_SlowObservationsCanCompleteBeforeTheHardDeadline`, `PlacementServiceObservationBoundaryTests.SelectedUnitProof_DoesNotStartAnotherObservationAfterTheHardDeadline`, `PlacementServiceObservationBoundaryTests.PanelDismissal_SlowHiddenObservationsCompleteWithoutAnotherClick`, `ChallengeActionRetryTests.SlowActionProof_CannotExceedMaximumParkingAttempts`, `StageNavigationTransactionStateTests.PendingSlowConfirmationBlocksAlternateNavigationInput`, `ExpeditionRecoveryTransitionLoopTests.CyclingStatesStillStopsAtTheOverallAttemptCap`, stable-navigation tracker tests, and startup-preflight tests.

### GB-031: Follow the current Expedition map-card rail phase

- Status: **Field confirmed** and **product safety contract**.
- Entry: the current full-screen Expedition selector exposes its live lower-left **Select Stage** action and one selected map card with a cyan perimeter.
- Variation: environmental lighting and the selector animation can move the complete three-card rail a few pixels vertically while bright green map artwork remains inside the nominal card perimeters.
- Action: evaluate all three map-card perimeters at one shared vertical offset from `-6` through `+6`. Choose the map only from the offset with the strongest valid cyan isolation while retaining the minimum selected-pixel and runner stability requirements.
- Exit: classify `map_select`, identify the selected map and difficulty, and use the current selector's owned actions.
- Do not: lower the cyan isolation threshold, move each card independently, infer selection from map artwork or localized names, or fall back to legacy selector coordinates on an unrecognized current layout.
- Failure rule: if the live Select Stage action, minimum cyan population, or separation from the other two cards is absent, return no selected map and send no selector input.
- Evidence: a beta.28 Deep Debug session reviewed on 2026-07-27 captured a canonical 808 by 611 Map 1 selector under green lighting. The fixed regions counted mutable perimeter artwork as `288`, `315`, and `373`; a shared offset restored the selected Map 1 isolation to `447`, `246`, and `255`. The old workflow retained stale `play` state and clicked the former Play action thirteen times until the user canceled.
- Protected by: `CurrentExpeditionSelectorTests.Selector_ToleratesSharedVerticalPhaseAndGreenLighting`, selector action tests, and the complete cross-state golden corpus.

### GB-032: Apply the selected unit's Auto Upgrade priority

- Status: **User confirmed** and **product safety contract**.
- Entry: by default, a placement click has produced two consecutive selected-unit-panel confirmations for the configured unit. Advanced mode may explicitly waive placement or reconfiguration proof as documented in GB-024.
- Action: after the configured targeting taps, send the Auto Upgrade Unit key zero times for Off or exactly one through six times for Priority 1 through Priority 6. Remember that state per stable placement ID. A later Reconfigure Unit action cycles forward through Off and Priority 1 through Priority 6 using only the modular tap count needed to reach its requested state. For example, Off to Priority 3 sends three taps, Priority 3 to Priority 5 sends two, and Priority 5 to Off sends two. Keep focus and cancellation checks between taps.
- Exit: dismiss the selected-unit panel through the shared bounded safe-corner path before advancing to another placement.
- Do not: send Auto Upgrade before positive selected-unit proof unless the setup explicitly opted out through the matching advanced responsibility, infer the priority from text or color, reuse the global Auto-Upgrade Placed Units setting, hold the key to normalize an already-known state, or continue remaining taps after cancellation.
- Compatibility: beta.31's saved and shared boolean `auto_upgrade` value migrates true to Priority 1 and false or missing to Off. New saves write the named priority.
- Failure rule: an invalid priority or unset Auto Upgrade Unit key stops before placement input that would require it. Cancellation stops before another tap or placement and relies on the shared atomic key-tap primitive to release the key.
- Evidence: the zero-through-six placement key count is user-confirmed behavior from 2026-07-27. The seven-state forward reconfiguration cycle is user-confirmed behavior from 2026-07-29.
- Protected by: `PlacementServiceTests.Playback_AppliesConfiguredAutoUpgradePriority`, `PlacementServiceTests.Playback_CancellationStopsRemainingAutoUpgradePriorityTaps`, `PlacementPlaybackAttemptTests.ReconfigureAutoUpgrade_UsesRememberedForwardCycle`, legacy placement JSON tests, and Fast share round-trip tests.

### GB-033: Close Roblox chat before Lobby or match input

- Status: **Field confirmed** from 72 canonical observations across open and closed chat states.
- Owner: the fixed Roblox chat button centered at client coordinate `(139, 34)`. Badge-independent speech-bubble top, side, and tail structure owns the glyph; its stable body fill then distinguishes the opaque open state from the outlined closed state. A notification badge does not itself mean chat is open, and a microphone in the nominal chat slot remains unknown rather than closed. The optional voice-chat microphone or headset control outside this slot is also outside the proof. The button position does not vary with Anime Expeditions UI Scale.
- Entry: after a workflow has acquired a verified Lobby, or after a mode has reached its verified prestart screen and before Settings, placement, or manual-recording input.
- Action: require the same open or closed indicator across two consecutive canonical frames. Closed completes without input. Open authorizes one acknowledged click at the detected button, clears prior stability, and requires two fresh closed frames. An ignored click may be retried once.
- Failure rule: an unknown, changing, missing, or noncanonical indicator never authorizes a click. Exhausting the observation budget or two verified clicks raises the typed UI-unavailable failure so normal diagnostics and configured recovery remain available. Cancellation sends no additional input.
- Evidence: the reviewed private datasets contained 33 closed-chat frames and 39 open-chat frames. The committed fixtures retain only the exact redacted button region on a black canonical canvas; they contain no world view, account identity, or chat content.
- Protected by: `RobloxChatButtonDetectorTests`, `RobloxChatPanelNormalizerTests`, startup-preflight tests, `MatchLobbyNavigatorTests`, resource-refuel tests, recovery tests, and the full cross-state detector corpus.

### GB-034: Wait for an actionable Upgrade Unit control

- Status: **User confirmed** and **product safety contract**.
- Entry: an Upgrade Unit Match Step has reopened its stable placement identity and the selected-unit panel is visible.
- Action: when Upgrade readiness is enabled, require two fresh frames of the same actionable control state before each requested keypress. A green Upgrade Unit control authorizes exactly one configured Upgrade Unit keypress. The ordinary narrow gray control means the upgrade is unaffordable; wait without input until it becomes green. The wider gray **Maxed** control means no further upgrade exists; complete the action without sending its remaining requested keypresses.
- Timing: wait no longer than three minutes for each requested upgrade. Preserve focus and cancellation checks before every capture and keypress, and allow slow capture observations to consume that same bounded wall-clock interval without multiplying input.
- Advanced opt-out: disabling Upgrade readiness sends the configured number of Upgrade Unit keypresses directly after the coordinate click, without selected-panel or affordability proof.
- Failure rule: two consecutive frames without the selected-unit panel, an unknown control until the deadline, a lost Roblox window, or cancellation sends no additional Upgrade Unit input and exits through the typed operation failure.
- Evidence: four canonical passive datasets supplied on 2026-07-29 cover two green affordable controls, a narrow gray unaffordable control, and the wider gray Maxed control. Forty frames across those datasets retain the same state-specific geometry.
- Protected by: `UpgradeUnitReadinessDetectorTests`, `PlacementUpgradeActionPlaybackTests`, `PlacementPlaybackAttemptTests.UpgradeStep_PressesUpgradeConfiguredNumberOfTimes`, selected-panel detector tests, and the complete cross-state detector corpus.

### GB-035: Reconcile and complete deterministic Mythic Bounties

- Status: **Field confirmed** and **product safety contract**.
- Entry: from three stable Lobby observations, open Events through its detected Lobby action, then locate the Bounty Board row from its copper rail plus the independently owned **Events** header. The row may be unhovered, hovered, selected, or shifted down by Beginner's Path. Require the same live row action in two fresh observations before clicking it; if Events directly restores the complete Bounty Board, accept that verified destination without a second click. Own the opened Board from the aligned **Back + Calendar** action rail and the bounded **Bounty Board** title. The ordinary title image proof uses its wide multi-row gold structure; a local glyph recognizer may corroborate that same crop after a small raster shift or hue change, but title recognition alone never owns the Board. Do not require the changing cards, bronze board body/top rail, selected Event row, or textual daily counter. The board can expose up to five usable cards after one bounded horizontal scroll. Derive that usable set from stable live card actions rather than the textual daily counter or a fixed slot count: yellow **Reroll** owns an active rerollable card, green **Claim** owns a completed card, and a card with neither action is dimmed and receives no input. A dimmed card may occupy any slot. An empty usable set is accepted only when the Board detector itself sees no yellow or green actions; nonempty actions that do not map to the requested scrolled geometry mean the scroll was ignored or unsettled and must fail boundedly rather than clearing saved state.
- Mythic proof: click only a live yellow card reroll action and wait at least 200 milliseconds before evaluating the response. An ordinary card rerolls directly back to the board. A Mythic opens the owned reroll-confirmation dialog. Cancel that dialog, reacquire the board, and classify only the card-anchored `#1` through `#10` suffix; quest order, Mythic title text, stars, and either randomized paper shape are not identity evidence.
- Parking policy: the task editor exposes no parking control. The fixed non-viable parking limit is zero, so every always-non-viable Mythic is rerolled to favor faster overlapping work. Public-beta saved parking values are discarded and omitted on the next save. Conditional Bounties 7, 9, and 10 remain parked through an ordinary Challenge cooldown, but are rerolled when the daily Challenge limit makes that objective impossible during the current UTC day. Stop scanning later slots once every unique Mythic retainable under the current Challenge policy has already been observed; at the daily Challenge limit, active #2, #4, #5, and #6 in slots one through four make slot five unnecessary.
- Gold boundary: the explicit red **You need 1000 Gold to reroll this bounty!** banner is authoritative and stops new rerolls immediately. If that banner cannot be observed, 100 ordinary rerolls without reaching a Mythic or the same confirmed Mythic remaining after four confirmed rerolls is bounded fallback evidence. Finish already-active viable objectives, then exclude only Bounty until the next user-started Macro operation. Do not restart Roblox or repeatedly retry the board to manufacture Gold.
- Objective action: the ten Mythic definitions are deterministic. Bounty 2 requires Flower Forest Infinite wave 30, School Grounds Infinite wave 30, and Spirit City Raid Act 1. Bounty 10 requires five regular Challenges, School Grounds Story Act 1 on Hard, and School Grounds Infinite wave 15; it is conditionally viable under the same Challenge availability boundary as Bounties 7 and 9. Merge incomplete Infinite objectives by map and choose the highest target so one match covers every lower objective on that map; do not merge similarly named Flower Forest and Fairy King Forest routes. Bounty 5's Rose Kingdom waves 15 and 45 therefore use one wave-45 route, and an active Bounty 9 shares that same match. Complete Story Hard, Spirit City Raid Act 1, and regular Challenge counts through their existing production runners. The regular-Challenge leg reuses the five required Story Infinite Placement Setups by validating the saved Story target, creating a Challenge-targeted runtime copy, and leaving the saved model unchanged. After the once-per-operation startup reconciliation succeeds, finish every currently executable active objective before claiming or rerolling, even when one active Bounty becomes complete earlier. After a partial objective, continue the next locally planned route directly from the verified Lobby. A process-recovery re-entry after that successful reconciliation likewise resumes persisted executable work without reopening the board first.
- Wave exit: an Infinite objective is credited by the game when target wave `N+1` starts; remain until `N+2` for a safe exit. Prefer two stable exact `N+2` readings. If exact image recognition misses, accept only three strictly increasing later wave readings. A duplicate does not advance the sequence; an unknown, regression, or reading at or below `N+2` resets it. Then use the existing verified in-match Lobby door flow. That verified Lobby observation invalidates the same-process Fast camera cache, so the next route prepares its camera again.
- Claim and persistence: the first Bounty selection of every user-started Macro operation must open and process the live board before persisted active entries can authorize route input. That initial transaction claims every live completed card, reconciles all observed active Mythic numbers, and performs the normal bounded reroll/parking policy. Mark the operation reconciled only after the resulting state is saved; a recoverable failure before that point retries the initial transaction, while recovery after it resumes active work. Reopen the board later when no executable active route remains and at least one complete Bounty is claimable; then claim and reroll as one board transaction. Claim only a live completed-card action whose `#1` through `#10` identity is also recognized, dismiss its owned reward overlay, and require two fresh Board observations that agree on the slot's settlement. A dimmed slot removes that Bounty number from the reroll pool for the remainder of the UTC day; a live yellow Reroll action leaves the number available. Both outcomes increment the daily claim count and remove the completed active progress. Dimmed-number exclusion also lowers the reachable non-viable parking target when the number was one of the four always-reroll Bounties. If Challenge inspection hands Bounty ownership back at the shared game-mode selector, close each verified Play layer, wait for the stable in-match door, complete the existing door-confirmation transaction, and separately prove Lobby before clicking Events. The green Claim action is forty pixels left of that card's ordinary Reroll center, so normalize both actions to one card anchor for slot ownership and number recognition while preserving the live Claim center for the click. Persist the UTC day, claimed count, dimmed-number exclusion set, active unique numbers, and deterministic objective counters locally for the one Roblox account assumed per Windows user. The public beta.45 through beta.47 Bounty schema is intentionally reset because the user confirmed that no deployed Bounty state requires preservation; translating its incorrect Bounty 2 Fairy King Forest key would falsely preserve Flower Forest progress. UTC midnight resets the ten-claim count and dimmed-number exclusion set; unfinished current-schema active cards persist and are reconciled because the board itself does not randomize them on reset. Bounty state and progress never enter share codes.
- Failure rule: an unknown board, moving or absent card action, unrecognized suffix, ambiguous confirmation, failed scroll, malformed embedded wave template payload, or other missing owner evidence sends no fallback coordinate input and raises the typed bounded UI failure. Number recognition evaluates complete `17×7` suffix support first, accepting at most one raster-pixel difference with a three-pixel runner-up margin. Exact reviewed raster variants may join that same distance competition only inside the action-owned title position and retain the same runner-up margin; they never bypass ambiguity checks. When that exact path fails, an action-owned glyph fallback requires either `0.90` Dice with a `0.03` runner-up margin or `0.80` Dice with a stronger `0.08` margin, plus a center within four pixels of the reviewed title position; ambiguous evidence remains unknown. Insufficient-Gold recognition requires both the neutral message and red alert backing. The embedded 0-through-100 wave templates must decode to exactly 101 fixed-size records before recognition can run; matching searches only ±1 pixel around each of the two reviewed counter locations and requires 70% template coverage plus `0.72` Dice so blank and partial regions remain unknown. A template match is ignored unless the independently detected gameplay hotbar, Unit Manager, and Stage Info controls own the same frame. The ordinary scheduler may use configured process recovery; insufficient Gold and completed wave timing are normal Bounty outcomes, not restart candidates.
- Evidence: reviewed canonical 808 by 611 captures from 2026-07-30 cover every number across all five slots and four randomized paper arrangements, Lobby-to-board navigation, repeated rerolls through Mythic confirmation and cancel/confirm paths, explicit insufficient-Gold banners and a 500-Gold counter, board-to-Lobby, in-match Infinite-to-Lobby, and claiming. Follow-up claim captures cover a dimmed card at every horizontal slot plus the fully dimmed `0/10` board; across all 95 settled Board frames, every live yellow/green card remained detected and no dim target exposed an action. A claim sequence also shows the completed card turning gray immediately after its reward is dismissed. Separate stable wave-counter captures cover every wave from 0 through 100, and a later no-voice capture moves the same counter from the retained `(389,48)` region to `(421,28)` while preserving the gameplay owner controls. The retained and no-voice counter fixtures also prove that the active layout is distinguished locally by its blue Wave badge and dark pill; red Enemies and yellow Time rows at the inactive anchor cannot own a wave reading. A 2026-08-01 beta.49 Bounty trace proves that the first conditional Bounty Challenge route loaded a valid Story Infinite setup but passed its Story target unchanged into Challenge compatibility validation, stopping immediately after board reconciliation. A later beta.45 Macro trace records Challenge Victory, verified cooldown inspection, correct Back transitions from game-mode selector through retained party into the active match, and then an immediate Bounty Lobby assertion without any in-match door click. Recovery restarted Roblox and re-entered Bounty with the local board-reconciliation flag reset, causing an unnecessary second board visit. A beta.46 field trace records a fresh user-started Macro operation trusting persisted Bounty 5 work and opening Rose Kingdom Infinite without any Events or Bounty Board observation; this establishes the once-per-user-operation reconciliation boundary.
- Protected by: `BountyBoardDetectorTests`, `BountyBoardLayoutTests`, `BountyNumberRecognizerTests`, `WaveCounterRecognizerTests`, `StageBountyWaveOwnershipTests`, `BountyModelTests`, `BountyPlannerTests`, `BountyIntegrationTests`, `BountyOperationSessionTests`, `BountyLobbyHandoffNavigatorTests`, `BountyWaveCompletionTrackerTests`, the Bounty add-task dark/light snapshots, scheduler recurring/session-exclusion tests, share-dependency tests, and the complete cross-state detector corpus.

## Reusable evidence workflow

1. Capture the complete attempt with deep debug, or pair a passive diagnostic capture with an exact user-described manual input sequence.
2. Generate timestamped contact sheets with `scripts/New-DiagnosticContactSheet.ps1`. Use enough adjacent frames to include the entry state, input response, intermediate animation or screen, and exit state.
3. Correlate `events.jsonl` input/state records when present. For physical inputs absent from passive logs, explicitly label the action as user reported.
4. Compare the sequence with this ledger. Preserve confirmed behavior unless stronger new evidence shows the game changed.
5. Add or update an entry using the template below.
6. Encode the transition as a policy or workflow regression test. Detector changes additionally require reviewed 808 by 611 fixtures and the full cross-state corpus.
7. Cite the ledger entry in non-obvious code comments or test names so later refactors retain the reason for the ordering.

## Entry template

```text
### GB-NNN: Short behavior name

- Status: Field confirmed | Release retained | Unverified.
- Entry: visible/detected starting state.
- Action: exact key, click, or wait.
- Intermediate: observable transition states.
- Next action: follow-up input, if any.
- Exit: verified destination state.
- Do not: known unsafe shortcut or ordering.
- Failure rule: bounded safe behavior when the transition does not occur.
- Evidence: date, app build, and capture type without private local paths.
- Protected by: regression test names and fixture locations.
```
