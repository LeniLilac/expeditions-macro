<UserControl x:Class="ExpeditionsMacro.DetectorViewer.Controls.EvidencePane"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:icons="clr-namespace:ExpeditionsMacro.DetectorViewer.Controls">
    <Border Background="{DynamicResource SurfaceBrush}"
            BorderBrush="{DynamicResource BorderBrush}"
            BorderThickness="1,0,0,0">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>
            <Border Padding="16,15,16,13"
                    BorderBrush="{DynamicResource BorderBrush}"
                    BorderThickness="0,0,0,1">
                <StackPanel>
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>
                        <StackPanel>
                            <TextBlock x:Name="DetectorNameText"
                                       Style="{StaticResource SectionTitle}"
                                       Text="Select a detector" />
                            <TextBlock x:Name="DetectorDescriptionText"
                                       Margin="0,4,12,0"
                                       Style="{StaticResource Caption}"
                                       Text="Choose a catalog item to evaluate this frame." />
                        </StackPanel>
                        <Border x:Name="ResultStatusBadge"
                                Grid.Column="1"
                                Padding="8,4"
                                Height="26"
                                Background="{DynamicResource RaisedBrush}"
                                CornerRadius="3">
                            <TextBlock x:Name="ResultStatusText"
                                       FontSize="12"
                                       Foreground="{DynamicResource MutedBrush}"
                                       Text="Waiting" />
                        </Border>
                    </Grid>
                    <Grid Margin="0,14,0,0">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                        </Grid.RowDefinitions>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>
                        <StackPanel>
                            <TextBlock Style="{StaticResource Caption}"
                                       Text="Final state" />
                            <TextBlock x:Name="FinalStateText"
                                       Margin="0,2,8,0"
                                       FontWeight="SemiBold"
                                       Text="Not available"
                                       TextTrimming="CharacterEllipsis"
                                       ToolTip="{Binding Text, RelativeSource={RelativeSource Self}}" />
                        </StackPanel>
                        <StackPanel Grid.Column="1">
                            <TextBlock Style="{StaticResource Caption}"
                                       Text="Confidence" />
                            <TextBlock x:Name="ConfidenceText"
                                       Margin="0,2,8,0"
                                       FontWeight="SemiBold"
                                       Text="Not available" />
                        </StackPanel>
                        <StackPanel Grid.Row="1"
                                    Margin="0,9,0,0">
                            <TextBlock Style="{StaticResource Caption}"
                                       Text="Decision gate" />
                            <TextBlock x:Name="DecisionThresholdText"
                                       Margin="0,2,8,0"
                                       FontWeight="SemiBold"
                                       Text="Not available"
                                       TextTrimming="CharacterEllipsis"
                                       ToolTip="{Binding Text, RelativeSource={RelativeSource Self}}" />
                        </StackPanel>
                        <StackPanel Grid.Row="1"
                                    Grid.Column="1"
                                    Margin="0,9,0,0">
                            <TextBlock Style="{StaticResource Caption}"
                                       Text="Action" />
                            <TextBlock x:Name="ActionText"
                                       Margin="0,2,0,0"
                                       FontWeight="SemiBold"
                                       Text="Not available"
                                       TextTrimming="CharacterEllipsis"
                                       ToolTip="{Binding Text, RelativeSource={RelativeSource Self}}" />
                        </StackPanel>
                    </Grid>
                </StackPanel>
            </Border>
            <Border Grid.Row="1"
                    Padding="14,10"
                    Background="{DynamicResource CanvasBrush}"
                    BorderBrush="{DynamicResource BorderBrush}"
                    BorderThickness="0,0,0,1">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>
                    <icons:ViewerIconControl x:Name="CoverageNoteIcon"
                                             Width="16"
                                             Height="16"
                                             Margin="0,1,8,0"
                                             Foreground="{DynamicResource MutedBrush}"
                                             Icon="Info" />
                    <TextBlock x:Name="CoverageNoteText"
                               Grid.Column="1"
                               Style="{StaticResource Caption}"
                               Text="Exact production geometry appears here when the selected detector exposes it." />
                </Grid>
            </Border>
            <DataGrid x:Name="EvidenceGrid"
                      Grid.Row="2"
                      AutoGenerateColumns="False"
                      SelectionMode="Single"
                      SelectionUnit="FullRow"
                      SelectionChanged="EvidenceGrid_SelectionChanged"
                      ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                      ScrollViewer.VerticalScrollBarVisibility="Auto">
                <DataGrid.Columns>
                    <DataGridTemplateColumn Header="Status"
                                            Width="90">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <TextBlock FontWeight="SemiBold"
                                           Text="{Binding StatusText}">
                                    <TextBlock.Style>
                                        <Style TargetType="TextBlock">
                                            <Setter Property="Foreground"
                                                    Value="{DynamicResource MutedBrush}" />
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding IsPassed}"
                                                             Value="True">
                                                    <Setter Property="Foreground"
                                                            Value="{DynamicResource SuccessBrush}" />
                                                </DataTrigger>
                                                <DataTrigger Binding="{Binding IsFailed}"
                                                             Value="True">
                                                    <Setter Property="Foreground"
                                                            Value="{DynamicResource ErrorBrush}" />
                                                </DataTrigger>
                                                <DataTrigger Binding="{Binding IsNotExposed}"
                                                             Value="True">
                                                    <Setter Property="Foreground"
                                                            Value="{DynamicResource WarningBrush}" />
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </TextBlock.Style>
                                </TextBlock>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                    <DataGridTemplateColumn Header="Check"
                                            Width="*">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <TextBlock Text="{Binding Label}"
                                           TextWrapping="Wrap" />
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                </DataGrid.Columns>
            </DataGrid>
            <Border x:Name="SelectedCheckDetail"
                    Grid.Row="3"
                    MaxHeight="190"
                    Padding="14,12"
                    Background="{DynamicResource CanvasBrush}"
                    BorderBrush="{DynamicResource BorderBrush}"
                    BorderThickness="0,1,0,0"
                    Visibility="Collapsed">
                <ScrollViewer VerticalScrollBarVisibility="Auto">
                    <StackPanel>
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>
                            <TextBlock x:Name="SelectedCheckNameText"
                                       Style="{StaticResource SectionTitle}"
                                       Text="Selected check" />
                            <TextBlock x:Name="SelectedCheckStatusText"
                                       Grid.Column="1"
                                       Margin="12,0,0,0"
                                       FontWeight="SemiBold"
                                       Foreground="{DynamicResource MutedBrush}"
                                       Text="Observed" />
                        </Grid>
                        <TextBlock Margin="0,9,0,0"
                                   Style="{StaticResource Caption}"
                                   Text="Expected" />
                        <TextBlock x:Name="SelectedCheckExpectedText"
                                   TextWrapping="Wrap" />
                        <TextBlock Margin="0,8,0,0"
                                   Style="{StaticResource Caption}"
                                   Text="Measured" />
                        <TextBlock x:Name="SelectedCheckMeasuredText"
                                   TextWrapping="Wrap" />
                        <TextBlock Margin="0,8,0,0"
                                   Style="{StaticResource Caption}"
                                   Text="Gate" />
                        <TextBlock x:Name="SelectedCheckGateText"
                                   TextWrapping="Wrap" />
                    </StackPanel>
                </ScrollViewer>
            </Border>
        </Grid>
    </Border>
</UserControl>
